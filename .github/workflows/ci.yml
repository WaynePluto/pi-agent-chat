name: CI

on:
  push:
    branches:
      - master
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.20.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      # The repository currently does not track pnpm-lock.yaml by convention.
      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Verify bundle and smoke load
        run: pnpm verify

      - name: Check VSIX contents
        shell: pwsh
        run: |
          pnpm exec vsce ls --no-dependencies --readme-path README.md
          pnpm exec vsce package `
            --target linux-x64 `
            --no-dependencies `
            --no-rewrite-relative-links `
            --readme-path README.md `
            --out pi-agent-chat-ci.vsix
