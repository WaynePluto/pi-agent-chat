# AGENTS.md — pi-agent-chat

非官方 VS Code 插件：在侧边栏提供 Pi Coding Agent 的聊天 UI。agent 能力来自官方 SDK `@earendil-works/pi-coding-agent`（不走 RPC，不要求用户安装 pi CLI）。

**插件是 pi 的另一个宿主，不是 CLI 的前端。** 选了 SDK 而非 RPC，就意味着宿主环境不同（扩展宿主 vs 终端进程），「与 CLI 能力面完全一致」从来不是可达目标——CLI 生态里任何依赖终端宿主的扩展在这里都会失效。因此兼容边界划在**数据面**而非能力面，见下面第一条红线。

## 架构速览

- `src/extension.ts` — 插件入口：注册 webview view（`piAgentChat.view`）、命令、诊断；`ChatViewProvider` 懒启动 `PiRuntime` + `ChatBridge`。
- `src/agent/runtime.ts` — SDK `AgentSessionRuntime` 薄封装；负责 session 新建/切换/fork 与 extension 重绑定；扩展 UI 的 `ctx.ui.notify` 经 sink 转到 transcript。
- `src/agent/bridge.ts` — 双向翻译层：SDK 事件 → `HostMessage`，webview 消息 → runtime 操作；session 历史回放。
- `src/agent/auth.ts` — 登录/登出流程：`ModelRuntime.login()` + `AuthInteraction` 映射到 VS Code 原生对话框。
- `src/agent/commands.ts` — 斜杠命令目录与内置命令分发（命名对齐 CLI）。
- `src/agent/model-picker.ts` — 两层模型选择：`buildModelCatalog()` 供 composer 快捷菜单列出范围内模型，`pickModel()` 为完整原生 QuickPick（搜索、能力详情、⭐常用、📌默认），另有 `/scoped-models` 批量管理；常用模型写共享设置 `enabledModels`。
- `src/agent/model-config.ts` — 自定义供应商：打开共享的 `~/.pi/agent/models.json`（空文件写入带注释的 JSONC 模板，已有内容则插入一条新模板）、列出其中定义的 provider、以 `jsonc-parser` 删除单个 provider 条目；供应商列表首行的入口 + bridge 的保存监听共同构成「编辑即生效」。
- `src/agent/skills.ts` — 技能路径索引：把 `read`/`bash` 调用判定为「加载技能」或「技能资源」，供 transcript 与资源面板区分展示
- `src/agent/tool-details.ts` — 工具 `AgentToolResult.details` 跨界到 webview 前的清洗（排除清单 + 深度/条目/字符预算截断 + 不可克隆值剔除）。
- `src/agent/activity.ts` — 宿主侧「本会话中真正生效过」的判定，专供资源面板点亮 Context / Extensions 两栏（transcript 里看不到这两类活动）：上下文文件每轮都无条件拼进 system prompt，故按「本会话已发出过请求」判定；扩展按它订阅的事件（`Extension.handlers` 的 key）是否已被 emit 推断，外加 `onError` 直证。
- `src/agent/invocations.ts` — 提示词模板与扩展命令的归属判定：`prompt()` 会把 `/模板` 展开成正文、把扩展命令直接消费掉，所以归属只能在提交前从原文解析（回放时仅能靠无占位符的模板正文精确匹配），供资源面板点亮对应行。
- `src/agent/subagent.ts` — 插件在 pi 自带工具之外**唯一**提供的工具 `subagent`：并行子会话调度与逐路汇报（默认关闭，见红线）。
- `src/agent/scope.ts` — 子代理写入范围：路径前缀规范化、启动前重叠判定、`ScopeGuard`（越界拒绝 + 写入记账）。
- `src/agent/scoped-tools.ts` — 用 SDK 导出的工厂重建的同名 `edit`/`write`，仅替换文件操作层以实施范围强制（唯一强制点）。
- `src/agent/config.ts` — 插件自有能力的 VS Code 配置（宿主侧唯一读取点，workspace 级可覆盖），并提供打开 VS Code 设置界面用的 section id。
- `src/agent/resume.ts` — 自动重试放弃后的手动重发（「retry failed」通知上的重试按钮）。
- `src/shared/protocol.ts` — host ↔ webview 消息协议与共享常量，**必须保持零依赖**（webview 打包不能引入 Node 代码）。
- `src/shared/messages.ts` — 宿主侧文案的中英字典（含参数化模板），同样零依赖；宿主经 `agent/i18n.ts` 的 `t()`/`tf()` 取用，webview `i18n.ts` 也引用它以保持措辞一致。
- `src/webview/` — webview 前端（无框架，DOM 直操作），按面板拆分：`main.ts` 只做布局/接线/路由，`transcript.ts`、`composer.ts`、`sessions-view.ts`、`resources-view.ts`、`picker.ts`、`statusline.ts`、`search.ts`（transcript 搜索，高亮走 CSS Custom Highlight API，不改 DOM）各管一块，`shell.ts`/`store.ts`/`host.ts`/`collapsible.ts`/`bubble.ts`/`clipboard.ts`/`dom.ts` 为公共设施；`i18n.ts` 提供 zh/en 双语。
- `media/main.css` — 样式，颜色只用 VS Code 主题变量。

项目模块架构见 `ARCHITECTURE.md`；修改模块结构后请更新该文件。

## 关键约定

- `package.json` 的 `dependencies` 与 `devDependencies` 必须使用固定版本号，禁止 `^`、`~` 等版本范围；新增依赖使用 `pnpm add --save-exact`（`.npmrc` 已默认启用 `save-exact=true`）。`engines` 等兼容性声明不受此限制。提交前用 `pnpm check:dependencies` 检查。
- **兼容边界在数据面（红线）**：会话文件与配置完全复用 `~/.pi/agent/`（auth.json、models.json、settings、extensions、skills、agents），两个宿主随时能接手对方的会话；不引入插件私有的配置副本。插件**允许**拥有 CLI 没有的能力，但受两条约束：
  1. **插件独有工具的名字由插件决定，同名扩展工具按名字屏蔽**。插件不知道某个扩展注册的同名工具是怎么实现的、依赖哪个宿主的什么能力，所以一个名字在本窗口只能有一个含义：插件选的名字（例如 `subagent`）要么解析到插件自己的工具，要么（开关关闭时）根本不存在，**任何情况下都不会解析到扩展的同名实现**。这是对生态的**尽力兼容**而非完美兼容：对方宿主 resume 后，模型若模仿历史里的调用形式，可能对那边同名不同义的工具报参数错误——会话多半在一个宿主上跑完、跨宿主 resume 罕见，产品定位（SDK 宿主）下接受这个成本，不做更重的保证。
  2. **会话文件必须保持 CLI 可读可继续**。标准不是「CLI 能完整复现这个会话的能力」，而是**「CLI 能打开、能读懂、能接着往下做，只是不能再调用那个独有工具」**——且不承诺跨版本可读（内置 SDK 与用户 CLI 的版本漂移是上游格式演进的责任，插件不参与）。实质是**插件不成为第三个会话格式作者**：只写 SDK 写的东西，不加私有字段。推论：独有工具的**结果文本必须自解释**（脱离工具语境也能读懂发生了什么），不能只把信息放在结构化 `details` 里。
- **配置项归属（红线）**：**插件独有的能力 → 插件自己的 VS Code 配置；与 CLI 共享的能力 → 共享配置 `~/.pi/agent/`**。两个方向都不能跨：把独有能力的开关塞进共享 settings 是污染（CLI 会读到看不懂的键）；把共享能力分叉到 VS Code 配置会让两个宿主行为不一致，比没有这个功能更糟。模型、认证、扩展、技能、常用模型（`enabledModels`，语义对齐 CLI `/scoped-models`：显式 `provider/modelId` 列表，全选或空则清空）均属后者。VS Code 配置另有一个共享 settings 给不了的好处：**workspace 级覆盖**（同一用户可以在实验项目开启、在生产仓库关闭）。
- **工具集（红线）**：插件只在 pi 默认的 `read`/`bash`/`edit`/`write` 之外注册 `subagent` 一个工具（默认关闭，见下条），且**不调用 `setActiveToolsByName()`**（自定义工具在 `_buildRuntime({ includeAllExtensionTools: true })` 时已自动激活，手动重写激活集只会静默关掉扩展工具）。`grep`/`find`/`ls` 等 SDK registry 里的其他工具，一律由 `~/.pi/agent/extensions/` 下的 pi 扩展提供（CLI 与 GUI 共享同一份）。**再加第二个工具的门槛（三条全部满足才行）**：① **语义可在其他宿主重现**（见上面「VS Code 能力的使用边界」）；② **能用这个宿主的结构性优势立论**——CLI 宿主结构上给不了，而不是「用 VS Code API 能做」，也不是「在这里做更快」；**能用 pi 扩展实现的能力一律不满足本条**（扩展能 spawn 任何进程，且 CLI 与 GUI 共享同一份，插件再造一份只会产生行为漂移）；③ 满足互通红线的两条约束（若与扩展同名则该扩展工具被屏蔽、结果文本自解释）。图方便就加工具，会同时破坏会话互通和用户对两个入口的预期。
- **`subagent` 工具（红线）**：插件唯一的自有工具，能力是**可写的并行委派**——主代理一次派出多路子会话，各自在声明的路径范围内直接改工作区。它的立论是这个宿主的结构性优势（in-process 子会话共享认证与模型运行时、完整事件流可订阅、GUI 能把 N 路空间化呈现），CLI 宿主只能 spawn 独立进程 + `--no-session`，结构上给不了。名字就用生态里耳熟的 `subagent`（模型一看就知道是委派），因此同名扩展工具被屏蔽（见下）。以下设计决定改动前先读完：
  - **默认关闭**，由 VS Code 配置开启（激进功能，用户自主选择；workspace 级可覆盖）。另有两项：**最大并行数**（默认 3，硬顶 8）与**子代理默认模型**（留空则继承父会话）。默认 3 是三条曲线的交点（明显快过串行 / 人还跟得过来 / 写冲突概率尚低），硬顶 8 是在保护功能定义——N 大到无法观察时它就不再是「可观察的并行」。**生效时机是「下一次会话构造」而不是「下一个会话」**：工具集在会话构造时固定，`session.reload()` 保留宿主的 `customTools`，但 SDK 的 `AgentSessionRuntime` 在 new / switch / fork / import 与窗口启动时都会重跑工厂（`createRuntime` 里重读 `readSubagentConfig()`），所以**切走再切回**同一个会话也会带着新配置重建，历史不丢。`ChatBridge` 监听配置变更（防抖 300ms，设置界面是逐键写入的）并按会话状态分两种反应：**空会话直接重建**（`applySubagentConfigChange()` → `newSession()` + `attach()`），非空会话只发提示。空会话是唯一没有出口的状态——header 的「新建」按钮在空会话上本来就是禁用的，不重建就只剩重载窗口一条路；而它也是唯一重建零损失的状态（未落盘的空会话在磁盘上根本不存在，被命名过的空会话文件仍留在列表里），所以「不能在用户背后扔掉对话」这条顾虑在这里不成立。**比较对象是会话构造时那份配置**（`PiRuntime.builtSubagentConfig`）而不是上一次读到的值：重复保存同样的值不该付出一次重建。**三项值没有插件自己的表单**：header 设置菜单的「子代理」一项直接把 VS Code 设置界面定位到 `piAgentChat.subagent`（`workbench.action.openSettings`）。曾经做过一个 QuickPick 表单（先选写入作用域再逐项改），已删：它把一个单页面拆成了三层嵌套点击，而它能提供的东西（作用域、当前值、说明文字）设置界面本来就每样都有且更好。推论：描述文字归 `package.nls*.json`（设置界面由 VS Code 渲染，拿不到 `t()`），插件不再为这三项维护任何写入路径。
  - **新会话提示必须区分开关两态**：屏蔽扩展 `subagent` 的那段提示（见下）在开关关闭时不能说「改用本窗口的子代理」——那时会话里一个委派工具都没有，得直说并指路设置菜单。为此 `history` 消息携带 `subagent: { enabled, shadowedExtension? }`，且 `enabled` 取自**会话构造时**记下的 `ToolSetupRef`（不是当下的配置值），因为提示描述的是屏幕上这个会话怎么装配的。功能默认关闭且无扩展被屏蔽时不可见，故同一段落另有一条独立的「功能未开启」提示（无扩展语境，只指路「设置 → 子代理」），也属于 `emptySession()` 的条件段落；开启且无屏蔽时不提示（用户自己开的，资源面板里也看得见）。
  - **约束分三层，插件只做前两层**：机制层（schema 字段、并行上限、scope 重叠检查、越界写拒绝）是硬约束，由插件强制；事实层是工具 `description`，只陈述机制；策略层（何时该用、怎么切分、本项目允不允许）属于用户的 `APPEND_SYSTEM.md` / 项目 `AGENTS.md`，插件一个字都不写。**用户的提示词管不了机制**，所以可写并行的风险只能靠机制兜，不能靠劝告。
  - **上限值即 `maxItems`**：配置项的值直接生成到 schema 的数组上限里，一处配置同时是文档（模型事前可见，不会提交超额再被拒，省一轮往返）与强制（超了是结构化报错，模型能自我修正）。不要在 prompt 里重复一遍。
  - **scope 必填且必须机制化执行**：每路子任务必须声明可写路径范围。启动前静态检查各路是否重叠，重叠即拒绝启动；运行中子代理越界的 `edit`/`write` 直接拒绝。范围只是说明性字段而不强制，等于退化成 prompt，整个设计作废。让模型填字段本身就是逼它把「这两个任务真的独立吗」显式化，比任何忠告都有效。告知子代理自己的 scope 不算注入提示词：它描述本次运行的配置、作者是主代理（通过工具参数指定）、且有机制强制，同告知 cwd 是什么。
  - **scope 用路径前缀而不是 glob**（`src/agent/scope.ts`）：重叠检查必须是**可判定**的，而两个任意 glob 是否相交一般情况下不可判定——glob 形状的 API 会把「证明不重叠」惄惄降级成「猜不重叠」，而那正是 scope 要消除的东西。前缀同时让包含判定和重叠判定都变成字符串比较。
  - **强制点在 operations 层**（`src/agent/scoped-tools.ts`）：`CreateAgentSessionFromServicesOptions` 没有 `toolsOptions`，内置工具无法重配，所以子会话用 `excludeTools: ["edit","write"]` 排掉它们，再经 `customTools` 注入用 SDK 导出的 `createEditToolDefinition` / `createWriteToolDefinition` 建的**同名同 schema 同行为**版本，只替换 `EditOperations` / `WriteOperations`。不复刻任何 SDK 逻辑；记账也在这一层顺手完成。**读不受限**（子代理必须能理解它改不了的代码）。**推论：不得引入第二条写路径**——任何绕过 `edit`/`write` 的写入工具（比如某个基于 `workspace.applyEdit` 的 rename）都会击穿 ScopeGuard，「唯一强制点」就不再唯一，scope 的整个设计价值随之作废。
  - **已知缺口：`bash` 写文件无法拦截也无法记账**。shell 命令写不写、写哪里不跑就不可判定（`BashToolOptions.spawnHook` 只能改 command/cwd/env）。汇报里**必须明写这一点**，不能让模型以为文件清单是完整的。
  - **直接写工作区，不做隔离副本**：环境真实，构建与测试结果可信，行为与单代理一致。代价是失败时留下半成品，由 git 兜底（非 git 工作区是本方案的真实风险敞口）。
  - **失败照实汇报，不自动回滚，但必须逐路记账**：结果要写明每路的状态、scope、失败原因、**实际写过哪些文件**，以及失败路是否留下了半成品（这一项要显式标注，模型读到「失败」很容易默认「什么都没发生」）。**失败原因只列能机械判定的**（`error` / `stopped_by_user` / `stopped_with_run`）：「子代理主动放弃」除了措辞之外与正常结束无法区分，猜一个就是向主代理编造事实；越界次数作为**独立字段**无论成败都汇报（它是切分质量的信号），**并且必须连同被拒的路径一起给**（`ScopeGuard.deniedPaths`，去重 + 有上限）。理由：**有些改动天生跨切面**（重命名函数 + 改调用方），任何对目录树的划分都关不住它，而光给个计数等于让主代理猜「差在哪里」；给了路径，拒绝就从「挡住」变成「情报」，主代理能自己收尾或重新切分。**不引入文件锁来“修复”这个场景**（已评估并否决）：锁给的是互斥，而跨切面改动缺的是**原子性**（不变量跨文件，锁的粒度在文件）；又因为没有回滚，拿不到锁时阻塞会死锁且不能安全中断、失败则直接留下半个重构，等于把冲突检测从「开跑前、免费」挪到「跑到一半、不可撤销」。真要并行做跨切面改动，对的方向是隔离（git worktree + 合并）而不是互斥。记账不是回滚：回滚被否决了，记账是「交给模型判断」能成立的前提，否则就是让模型盲猜。
  - **不引入子代理角色文件（已评估并否决）**：曾复用 `~/.pi/agent/agents/*.md`（CLI subagent 扩展的角色定义），后按本条删除。理由：那份约定**不是 pi 的**——SDK 里没有任何 agents 加载器（skills / prompts / extensions / context 都有），目录、frontmatter 字段与 `tools`/`model` 语义全由那个扩展自己定义。消费它等于把某个第三方扩展的私有布局固化进插件的能力面，而插件同时还**屏蔽**着它的工具：一边声明「不看它怎么实现」、一边吃它的数据格式，前后矛盾；用户没装那个扩展时，该目录对 pi 而言更是毫无意义。角色提供的三样东西各归其位：systemPrompt 是**行为引导**，属于用户提示词（由主代理写进 `task`）；`model` 已是 schema 字段；`tools` 白名单是**机制**，要恢复就做成 schema 字段（宿主无关），不能靠外部目录约定。**推论：资源面板只列 pi 官方的资源类型。**
  - **模型解析按来源分两种失败方式**（`planModel()`，优先级：工具参数 > 子代理默认模型设置 > 父会话模型）。**主代理自己填的 `model` 找不到 = 硬错误**，且在任何一路启动前抛出：那是机械可判定的参数错误，告诉它才能自我修正。**用户配置的那一级（插件设置）找不到则降级**到父会话模型，并且**只提示用户**（`LaneNotice` → `ChatBridge.onLaneNotice()` → 主会话 transcript 的 status 卡片），主代理的汇报里一个字都不写。理由：那个模型不是主代理选的，它也改不了拼写或去登录供应商，报给它只会让它去「修正」自己没发过的参数；而整路任务因为一个笔误全废，代价远大于换个模型跑完。`diagnostics.ts` 的 `subagent model selection` 自检项钉住这两个方向。
  - **用户只跟主代理对话**：子会话面板只读、无输入框；对子代理只有「看」和「停某一路」两个动作（都是对**运行**的控制，不是对话）。**对话线永远只有一条**，这是并行功能最大认知风险（N 个 agent 在跑，用户该跟谁说话）的正解。推论：启动并行后视图**停留在主会话**（串行版那种自动跳转在并行下无解），并行卡片就画在主会话 transcript 的工具卡片里（不引入新导航层级，且跑完后天然留在历史里可回看），点击某一路才进入其只读预览；
  - **实时进展行是命题成败所在**：主代理等待期间自己没有输出，主会话的全部活跃感来自那张卡片。每路必须持续显示「此刻在做什么」（当前工具调用 / 刚改的文件），都只写「运行中」等于看起来卡死，「可观察的并行」就成了空话。为此：卡片**默认展开**（`startToolCard` 里对这一个工具名特例，且必须在 `entry` 赋值后才 `setExpanded(true)`，render 回调会反读 `entry`）；`tool_update` 事件**带 `details`**（原本只有 `text`，不带的话卡片在整个运行期间都是静止的）；`subagent` 也因此**不在** `TOOLS_WITH_DEDICATED_CARDS` 里——它的卡片恰恰是用 `details` 画的。
  - **用户正停在某一路时跑完了不要自动弹回**，只在返回入口标记「主代理有新进展」（他是主动点进去的，弹走会丢失阅读位置）。停单路的汇报必须写明「用户主动中止」，否则主代理会好心重开一路，把用户刚踩的刹车松掉。
- **始终屏蔽扩展注册的 `subagent`，与开关无关**：屏蔽理由是**同名屏蔽**——本窗口自带 `subagent` 工具，这个名字归插件所有；与扩展的实现、它依赖哪个宿主的能力无关（插件既不认扩展身份，也不看它怎么实现）。两种开关态由机制各自保证：开关**开**时不排除、靠 SDK 工具注册表的覆盖语义赢（custom tool 在 `_refreshToolRegistry()` 中后写覆盖同名扩展工具，模型解析 `subagent` 永远拿到本窗口的工具）；开关**关**时经 `excludeTools` 把名字整个排除。排除集与 custom tools 都持久化在会话上，`/reload` 后同样成立。附带事实：扩展式 subagent 实现在本宿主本来就会静默失败（`ExtensionContext` 没有任何开子 session 的能力，扩展只能重新启动 pi，靠 `process.argv[1]` / `process.execPath` 自省定位——在扩展宿主里那指向 VS Code 自己的 `bootstrap-fork.js`（**它存在**，所以 `existsSync` 通过），结果是 spawn `Code.exe` 后 **exitCode 0 + 空输出**，模型拿着空结果继续推理），所以屏蔽不丢任何能力。状态很干净：开关关 → 一个委派工具都没有；开关开 → 只有本窗口的 `subagent`。**只按工具名，不认扩展身份、不看它怎么实现**；被屏蔽者经 `PiRuntime.shadowedSubagentExtension` → `history` 消息的 `shadowedSubagent` 字段告知用户。这条提示属于**新会话提示**（`emptySession()` 的条件段落，与 `systemPromptOverridden` 同机制）而非 transcript 事件：它描述会话怎么装配，不是会话里发生了什么；当成事件发会把 placeholder 顶掉（`applyEvent` 末尾无条件 remove）并凭空造出一个「运行中」的 work block。
- **不做 spawn CLI 的兼容垫片**：让扩展版 `subagent` 在这里跑起来只有两条路，都否掉。(a) 干预它的进程自省（改 `process.argv[1]`、往 `process.env` 塞 `ELECTRON_RUN_AS_NODE`）——进程级全局 mutation，会被所有子进程继承（包括用户每一次 `bash` 调用），且完全建立在猜测扩展实现之上，直接撞「不看它怎么实现」的红线。(b) 插件自己 spawn `pi`——依赖用户装了 CLI，撞「不要求安装 pi CLI」的定位，还带来版本偏移、认证/代理各走一遍、子会话黑盒。扩展版依赖的 spawn 机制只是它在 CLI 宿主里唯一能用的手段，是缺陷不是特性；它的 `agents/*.md` 角色数据同样不吸收，理由见上面「不引入子代理角色文件」。
- **不做通用的「依赖宿主的扩展」拦截**：判据不存在。静态扫描源码找 `process.argv` / `spawn` 是认实现且误报漏报都高；认扩展名/路径直接撞红线；让扩展自己声明宿主需求需要新 API（又是插件私有扩展 API）。`subagent` 是唯一的例外，且理由与宿主的失败模式无关：它是插件自有工具的名字，同名扩展工具必须屏蔽（见上），不涉及对扩展实现的任何判断。其他依赖宿主的扩展工具只能靠 README 的宿主边界说明让用户自己对号。
- **VS Code 能力的使用边界（红线）**：VS Code API 用在**呈现层**（面板、状态行、QuickPick、diff 视图）和**宿主职责层**（认证对话框、代理配置、插件自有能力的配置项），不用在 **agent 能力面**。判据是工具的**语义能否在其他宿主中重现**，而不是实现时碰没碰 VS Code API：「读取编辑器选区」「当前断点停在哪」在 CLI 宿主里**结构上不存在对应概念**，会让会话历史变成只在这一个宿主里有意义的东西，禁止；「查找符号引用」这类语义任何宿主都能实现（CLI spawn language server 即可），输出是 `文件:行:列` 纯数据，**不因实现手段而违规**。但**语义可重现只是必要条件，不是通行证**：它还得过下面「工具集」那条的结构性优势门槛——而**能用 pi 扩展做的能力就不具备结构性优势**（扩展跑在 Node 里，能 spawn 任何进程，且做完 CLI 与 GUI 共享同一份）。`subagent` 工具过关是因为它架在 **SDK 的 in-process 子会话**上，不是 VS Code API，CLI 宿主只能 spawn 独立进程 `--no-session`，结构上给不了。LSP / symbols / diagnostics 工具属于前一类——语义可重现但无结构性优势（扩展 spawn language server 即可，且 CLI 与 GUI 共享），**已评估并否决**；它唯一的真实差异是 VS Code 的 language server 已热索引，而那是性能差异不是能力差异，且扩展未激活时的空结果与「确实无引用」无法区分（危险的假阴性）。需要新能力时，扩展由用户经 pi 自己的包管理器安装（`pi install` / settings 的 `packages`，见 `docs/packages.md`）；插件**不代管、不捆绑、不安装**任何扩展。插件对扩展只做**渐进增强**，且增强只能架在 **SDK 原生、宿主无关的接口**上（`ctx.ui.setStatus` / `setWidget` / `notify` / 斜杠命令）：它们描述内容而非布局，每个宿主都欠它们一个渲染。**不得为某个具体扩展定制 UI**（不认它的注册表文件、不认它的命令名），也**不得自创插件私有的扩展 API**（那只是把耦合反转到扩展侧，且 CLI 无对等物）；真缺能力时推动上游给 `ExtensionUIContext` 加。**上游若为某类宿主能力给出了标准接口（例如「向宿主查询语言服务」），插件实现它就不再是私有能力，性质同 `setStatus`，红线自然放行。**
- **提示词（红线）**：插件不向 system prompt 注入任何**行为引导**。工具 `description` 只陈述机制事实（并行、隔离上下文、scope 会被强制、不可嵌套、结果如何返回），不写 `promptGuidelines`、不写「应该/不应该何时用」。防滥用、工具优先级、并行切分策略这类策略属于用户的 `~/.pi/agent/APPEND_SYSTEM.md` 或项目 `AGENTS.md`，CLI 和 GUI 自然共享。**两件事不算注入**：① schema 字段与上限（机制，且比 prompt 可靠——**约束应当落在 schema 上而不是 prompt 上**）；② 告知子代理本次运行的参数（scope、cwd 这类，作者是主代理）。
- **自定义供应商只有「编辑 models.json」一条路**：自定义接入点/模型/API key 属于共享配置（`~/.pi/agent/models.json`，见 `docs/models.md`），CLI 与 SDK 都不提供编辑 UI。插件在供应商列表首行给一个入口打开该文件：文件为空时写入带注释的整份模板，已有内容时向 `providers` 顶部**插入一条新的 provider 模板**（id 自动错开，存为未保存的编辑以便 Ctrl+Z 撤销，也避免未填写的占位供应商直接进模型列表）（pi 用 `stripJsonComments` 解析，注释是格式的一部分；文档语言设为 `jsonc` 以免 VS Code 报错），保存时经 `ChatBridge.reloadModelsConfig()` 做 `modelRuntime.refresh({ allowNetwork: false })` 并把 `getError()` 显示成 transcript 卡片（CLI 在启动/`/reload` 时也打印它，缺了它写错配置会静默失效）。**不做表单/向导**：models.json 的 schema 面太大（四种 api、十几个 compat 开关、cost 分层、thinkingLevelMap、modelOverrides），向导只能覆盖一小部分且用户最终仍要回到文件，同时它会变成 GUI 私有的配置编辑器，与 CLI 无对等物。允许的结构化写入**只有整条 provider 粒度的两个**：插入一条带注释的模板（文本插入，因为 `modify()` 写值会丢掉注释）与删除整个条目（供应商行的 🗑 按钮，`jsonc-parser` 的 `modify()`）；两者都用 `WorkspaceEdit`（保留注释与格式、与已打开的编辑器同步、可撤销），删除另经 `document.save()` 走与手改相同的重新加载路径；**不要再往里加字段级的写入**。注意 `jsonc-parser` 没有 exports map，esbuild 默认会选到它的 UMD 构建（内部 `require("./impl/format")` 无法静态解析，bundle 加载即报 Cannot find module），因此 `esbuild.mjs` 用 alias 钉到 `lib/esm/main.js`。
- 有意偏离 CLI 的一处：SDK 的 `AgentSession.setModel()` 会顺带改写 `defaultProvider`/`defaultModel`（CLI 语义是「选中即设为默认」）。插件把两者分开——切换模型只影响当前会话，默认模型只由模型选择器行内的 📌 按钮设置——所以 `PiRuntime.setModel()` 在调用后会把原默认值写回。改动这一段前先确认该语义。
- 模型与思考等级的**快捷菜单**画在 webview 里（`src/webview/picker.ts`，小弹层对齐 composer chip）：QuickPick 固定出现在窗口顶部，离被点击的控件太远。菜单只做切换：模型行只有名称 + 供应商，列表内容仅限常用模型（scoped），未配置时显示「无」而不是列出全部；搜索、能力详情、⭐常用、📌默认均由末尾「其他模型…」打开的原生 QuickPick 提供，`/scoped-models` 同理。宿主只负责数据与落盘（`models` / `openPicker` 宿主消息，`listModels` / `setModel` / `pickModel` / `setThinkingLevel` webview 消息）。
- **失败请求的手动重发**（`src/agent/resume.ts`）：一次请求没回来之后，继续任务的唯一办法曾是打一句「继续」——那条用户消息既进 transcript 也进模型上下文，却与任务无关。改为在一条带重试动作的通知上收尾：丢掉那条失败响应（**只丢 agent state，会话文件照 SDK 自己的做法保留**）再以空消息批走会话自己的 prompt 路径继续，等价于 SDK 自动重试在两次尝试之间做的两步，**不新增任何消息**（与互通红线无冲突：写入的仍然只是 SDK 会写的东西）。六条边界：
  - **必须走 `_runAgentPrompt` 而不是公开的 `session.agent.continue()`**：外层那圈循环才负责把会话标为 streaming、给这次尝试套上自动重试与自动压缩、并发出 `agent_settled`；直接调 agent 能把请求发出去，但 UI 会永远等一个不会来的 settle。
  - **私有入口必须特性探测**：探测不到就不提供这个动作（`supportsResume()`），升级后退化成「没有按钮」而不是「按钮失灵」；`diagnostics.ts` 的 `manual retry` 自检项钉住它。
  - **能不能重发只由宿主按会话状态判定**，不由按钮判定（通知会留在 transcript 里，点击天然可能过期），且只对「停在 `stopReason === "error"` 的响应上」成立：重发一轮正常结束的对话会把用户正在读的答案悄悄丢掉（`aborted` 同理：那是用户自己按的停止）。
  - **提供重试的时机是 `agent_settled`，不是 `auto_retry_end`**（`offerRetry()`）：只有那里自动重试/压缩/排队全部落定，「这一轮停在失败响应上」才是稳定事实，一条规则同时盖住三种失败（自动重试放弃 / 自动重试被关掉 / 压根不可重试的错误）；挂在 `auto_retry_end` 上只能盖住第一种，而且一次失败会长出好几个按钮。子会话不给：一路子代理挂了是汇报给主代理的事，用户对子代理只有「看」和「停」。
  - **回放时必须重新给出这个提议**（`withRetryOffer()`）：那条通知是 transcript **事件**，只存在于看见失败发生的那个窗口；重启后从会话文件回放只剩一条赤裸的 provider 错误（`Request timed out.`），无处可点——而使重试有意义的**状态本身是存活的**（失败响应仍是最后一条消息）。因此在 `postHistory()` 重算一次，且**只加在发给 webview 的副本上**，不写进内存历史（否则每次 attach 都会长出一条）；`diagnostics.ts` 的 `replayed retry offer` 自检项从会话文件一直钉到真实 post 出的 `history` 消息。
  - **带动作的通知不进 work block**（`ChatEvent` 的 `status.retry`）：work block 默认折叠，藏在折叠里的按钮不算「提供」。
- **扩展输出走 transcript**：pi 扩展的 `ctx.ui.notify` 不弹 VS Code 通知框（单行、会截断、脱离对话），而是经 `PiRuntime.setExtensionNoticeSink()` → `ChatBridge.emit()` 变成 `status` / `error` 卡片留在聊天区，多行自动折叠。扩展 handler 抛出的错误同理：`bindExtensions({ onError })`（SDK 的 interactive/rpc/print 三个 mode 都传，属应用入口职责）经 `setExtensionErrorSink()` 变成 transcript 里的 `error` 卡片，不再静默丢弃。两个 sink 都必须在首次 `bindExtensions()` 之前注入（`ChatBridge` 构造函数里），且闭包捕获被绑定的那个 session，子会话/preview 的隔离由 `emit()` 的 `histories` 自动生效。扩展命令执行期间（`extensionCommandDepth > 0`）标 `scope: "command"` 置于顶层并展开，其余（后台事件里的 notify）不标 scope、收进 work block 防刷屏。未接线 sink 时 notify 仍回退原生弹窗，不静默丢消息。
- **扩展命令上下文由宿主提供**：`ctx.newSession()` / `ctx.fork()` / `ctx.switchSession()` / `ctx.navigateTree()` / `ctx.reload()` 走 `bindExtensions({ commandContextActions })`，SDK 没有默认实现（缺省是“报告成功但什么都不做”的 stub，所以不接线就是静默失效），三个 mode 各自接线（最接近的是 `modes/rpc/rpc-mode.ts`），插件同理（`PiRuntime.commandContextActions()`）。会话替换有两条来源：**宿主发起**的（webview 动作 → `PiRuntime.newSession()` 等，调用方随后自己 `attach()`）与**扩展发起**的（在 SDK 内部完成，宿主只能经 `AgentSessionRuntime.setRebindSession()` 得知）；后者由 `SessionLifecycleSink.reattach()` 重建视图，前者靠 `replacingSession` 标志跳过，否则会 attach 两遍并重发一次 `session_start`。`navigateTree` 不换 session 对象、不触发 rebind 钩子，所以在 action 里显式 reattach。这组 actions **只给窗口正在显示的那个会话**（`bindSessionExtensions(..., { ownsSession: true })`）：子会话绝不能把父会话换掉。`ctx.reload()` 与 `/reload` 必须同一条路径（`SessionLifecycleSink.reload()`），否则两者的 UI 收尾会不一致。`diagnostics.ts` 的 `extension command context` 自检项钉住它：真实调一次 `ctx.newSession()`，会话要真的换掉且 reattach 恰好触发一次。
- **`setStatus` / `setWidget` 不是 TUI-only**：它们和 `notify` 一样描述内容而非布局，CLI 分别渲染在 footer 与编辑器上下方，所以插件也必须渲染（`setExtensionStatusSink` / `setExtensionWidgetSink` → `webview/statusline.ts` 的独立状态行 / `webview/widgets.ts` 的 composer 上下文本块）。它们曾落在 `createVsCodeExtensionUiContext` 的 no-op Proxy 里被静默吞掉——那是错误归类，会让用了这两个 API 的第三方扩展在 GUI 中无声失效。`setWidget` 的 component-factory 重载才是 TUI-only（基于 pi-tui 的 `Component`），检测到函数入参直接丢弃，不崩溃。这两类是**实时 UI 状态而非 transcript 历史**，因此不进 `histories`，按 sessionId 隔离、`attach()` 时清空由扩展重新发布（对齐 CLI 重新 bindExtensions 的行为），preview 下发空集。
- **工具 `details` 透传而非移植 `renderCall`/`renderResult`**：这两个钩子只产出 pi-tui `Component`，而 `Component` 唯一的出口是 `render(width): string[]`——已排版的 ANSI 行，**里面取不出内容**（作者意图在函数体的控制流里，是代码不是数据）；把 ANSI 转回 DOM 属于「TUI 呈现层特性」，不引入。真正结构化的是它们的**输入**：`args` 与 `AgentToolResult`。因此插件把 `details` 经 `agent/tool-details.ts` 清洗后透传，在 `webview/transcript.ts` 的 `renderDetailsBlock()` 画成**默认折叠**的通用树。三条边界：只给没有专用卡片的工具（`TOOLS_WITH_DEDICATED_CARDS` = pi 自带七个；`subagent` 虽有专用卡片但**不在列中**，因为它的卡片正是用 `details` 画的）；**不认任何扩展的 schema**（认了就是为具体扩展定制 UI，红线）；宿主必须清洗（details 是任意对象，含函数/循环引用会让 `postMessage` 的结构化克隆直接失败，超大 payload 会刷屏）。
- **webview 的「当前显示什么」是单一联合类型（红线）**：`bridge.ts` 的 `View` = `live`（主会话、可输入）/ `lane`（活的子会话，实时进展、可单独停）/ `replay`（从会话文件重放，`laneTitle` 非空则仍是子代理）。`displayedSession` / `inputDisabled` / `delegation` / `transcriptId` **全部从它派生**，切视图只经 `setView()`。这一条是代价换来的：它们曾是三个独立字段（`displayedSession`/`displayedLaneId`/`preview`），合法组合靠人每次记得同步，同一个 bug 修了三轮都只改对了链路的一段。**新增派生状态时不得新加平行字段**，否则等于把该类错误重新引入。
- **宿主侧状态必须由宿主侧测试守**：`scripts/smoke_webview.mjs` 手工构造 `ChatState`，绕过了 `postState()`，因此**对宿主侧 bug 完全不可见**——测试与实现共享同一盲区时，绿色是假的。`diagnostics.ts` 的 `view state` 自检项驱动真实 `ChatBridge.handleMessage()` 并读 `postState()` 实际 post 的内容，改视图逻辑后必须跟着扩充。
- **webview 的阅读位置按 transcript 记忆**（`transcript.ts` 的 `TranscriptViewState`）：展开的 work block、消息区滚动、`.work-body` / `.tool-body` 内部滚动。`applyHistory()` 每次全量重建 DOM，而子代理鼓励用户频繁进出，不记则每看一眼就丢一次位置。key 用**位置序号**（work block）与 **toolCallId**（卡片）；身份由 `history` 消息的 `transcriptId` 给出，两层 Map + LRU 上限 8（往返必然经过另一个 transcript，单层会必丢）。
- **正式消息只有「每个角色最新一条」默认展开**（`webview/bubble.ts`）：更早的**长**消息折叠成预览（CSS 裁剪 + 渐隐，不重建 DOM），短消息永不折叠——为省两行而多一次点击是纯负担。三条边界：① **是否够长按 Markdown 源文本判定**（行数阈值来自插件自己的设置 `piAgentChat.transcript.foldLines`，默认 14；无换行的长文本按每约 50 字符折算成一行计入同一预算，`0` 为永不折叠；经 `foldThreshold` 宿主消息推送，变更后由宿主重放 transcript 让已建气泡重新判定），不按渲染高度——无头快照里每个元素都测得 0，用测量就会与屏幕上不一致，回归也就测不出来；② **用户手动开合过的气泡（`pinned`）不再被自动折叠**，该决定按位置序号记进 `TranscriptViewState.bubbles`，与 work block 同一套记忆机制，切走再回来仍然作数；③ 渲染出的 Markdown 必须待在 `.bubble-content` 里，裁剪只能作用于它——徽章、页脚与 hover 动作条都在 `.bubble` 上，被一起裁掉就再也点不到了。
- **复制写剪贴板走宿主**（webview 的 `copyText` 消息 → `vscode.env.clipboard`，同 `openFile`）：webview 里的 `navigator.clipboard` 受焦点与权限限制，desktop / remote / 浏览器宿主表现不一致；按钮乐观回显✓，失败由宿主自己报。代码块的复制按钮在 `markdown.ts` 的白名单净化**之后**注入（marked 没有这个能力，也不为它引第三方依赖），因此包裹元素与按钮不在白名单里也能存活。
- **输入历史的方向键只在文本外层行被劫持**（`webview/composer.ts`）：↑/↓ 回溯/前进发送历史（webview 侧内存记录，全局共享、上限 100、连续重复不入栈，导航起始时暂存草稿并在回到最新时恢复；条目存**原文**与 `@` 引用 chips，两者一并回填，离开某个历史位置时把当前编辑写回该槽位，清空视为放弃不写回，发送即结束导航）。触发条件是「光标（无选区）在第一行/最后一行」而非文档绝对首尾：单行输入要对齐 readline 的肌肉记忆（每次按键走一条，否则回溯第二条起每次要按两次），而多行草稿中间的方向键必须留给光标移动；IME 组合中（`isComposing`）不触发，补全面板打开时补全优先。历史必须在 webview 侧记录：会话文件存的是展开后的正文，回放拿不到用户敲的原文。
- **基样式 `button:hover` 必须用 `:where()` 保持与 `button` 同特异性**（`media/main.css`）：写成直觉的 `button:hover:not(:disabled)` 会拿到 (0,2,1)，**盖过所有只用类选择器的 hover 规则**（(0,2,0)），于是卡片头、列表行、气泡里的小按钮这些静止时扭平的按钮，一 hover 就亮成主按钮的**蓝色方块**（而且它们自己的规则看起来完全正常，很难归因）。曾经用一份穷举类名的清单打过补丁，但那要求每个新增的扁平按钮都记得去登记。**hover 反馈一律用灰**（`--hover-bg-strong` / `list-hoverBackground`），蓝色只给**静止时就该是蓝的**东西（主按钮、链接文字、列表选中态、焦点环）。
- **代码块高亮用 highlight.js（而不是 shiki），颜色映射到 VS Code 主题变量**（`src/webview/highlight.ts`）：shiki 复现的是 VS Code 自己的 TextMate 语法，但要带上语法包 + WASM 正则引擎（体积以 MB 计、异步加载），**而且它同样拿不到用户主题的 token 颜色**（webview 只能拿到 workbench 颜色，拿不到 `editor.tokenColorCustomizations` 背后那套），差异不值这个体积。三条边界：① 用 `highlight.js/lib/core` + **手选语言子集**（全量 190+ 语法包对侧边栏是浪费）；② **只高亮 fence 声明的语言**，未标注/未注册一律保持纯文本——auto-detect 在聊天里常见的短片段上猜得很差，**错的颜色会被读成错的代码**；③ 颜色只用 `--vscode-*` 变量（symbolIcon / debugTokenExpression / charts 系列，逐个带 fallback），**不引 highlight.js 自带的主题 CSS**（硬编码颜色，不跟随明/暗/高对比度主题）。流式回答每帧都会重渲染整段 Markdown，所以高亮结果带缓存，超大代码块（`MAX_HIGHLIGHT_CHARS`）直接不高亮。
- 每次 `runtime.session` 被替换（new/resume/fork/tree）后必须重新 `bindExtensions()` 并重订阅事件 —— 走 `ChatBridge.attach()`。
- **启动时打开哪个 session：记住上次显示的那个，而不是磁盘上最新的那个**（`ChatViewProvider.startupSession()` ← `context.workspaceState`）。`SessionManager.continueRecent()` 挑的是「最近修改的文件」，两个状态它表达不了：① 用户切回了某个旧会话；② 用户停在**空的新会话**——新会话的 JSONL 直到第一条 assistant 消息才落盘（`_persist()` 的 `hasAssistant` 分支），所以这个状态在磁盘上没有任何痕迹，只能由宿主记住。因此 `BridgeHost.rememberSession(file)` 由 `ChatBridge` 在 `attach()` 与主会话事件里同步（`undefined` = 尚未落盘），宿主按 cwd 存进 `workspaceState`；`PiRuntime.create({ startup })` 的三态 `new` / `recent` / `file` 对应恢复、`file` 指向的文件不存在时降级为 `recent`（不能用 `SessionManager.open()` 直接开一个不存在的路径：它会静默新建一个钉在该路径的空会话，把用户删掉的文件重新造出来）。从未记录过（首次运行）仍走 `recent`，即 README 描述的 `pi --continue` 行为。`diagnostics.ts` 的 `startup session` 自检项钉住这三条。
- 不要与终端 Pi 同时 resume 同一个 session（JSONL 追加写无锁）。
- 代理与 HTTP dispatcher 与 CLI 保持行为一致：`src/agent/http.ts` 复刻 SDK 未导出的 `core/http-dispatcher.ts`（`applyHttpProxySettings` + `configureHttpDispatcher`）。代理优先级 **环境变量 > `~/.pi/agent/settings.json` 的 `httpProxy` > VS Code `http.proxy`**；前两级就是 CLI 的顺序，VS Code 一级只能填 CLI 会直连的空位，因此插件不会与 CLI 冲突。`httpIdleTimeoutMs` 改变后必须重建 dispatcher（设置菜单的 `apply` 钩子）。

### 「CLI 有、SDK 没导出」的能力如何取舍

本项目只消费 SDK 的**公开导出面**（exports map 仅 `.` / `./rpc-entry` / `./client`），在 GUI 层做体验优化。遇到「Pi CLI 有、但 SDK 没导出」的能力，按以下四类判定：

1. **TUI 呈现层特性** → 不引入。如 mermaid 渲染、主题选择器、`registerMarkdownTransformer`、`tuiMode`。GUI 有自己的呈现方式，要做也是独立设计，不是移植。
2. **SDK 内部纯逻辑 helper** → 不复刻。要么用平台/自己的等价方案，要么放弃该功能（例：`normalizeWindowsShellPath` 曾被复刻为 `agent/paths.ts`，后按本规则删除）。
3. **应用入口职责**（CLI 在 `main.ts` 层做、任何宿主都绕不开的事）→ 必须实现。插件是 `main.ts` 的**同位体**，不是它的消费者。验收标准是「与 CLI 行为一致」，且代码里必须注明对应的 SDK 源文件路径，便于升级时比对（例：`agent/http.ts`）。
4. **共享配置的语义**（`~/.pi/agent/` 里的设置项）→ 必须与 CLI 一致。同一份 `settings.json` 两边行为不同，比没有这个功能更糟。

配套约束：

- **禁止 deep import 进 `dist/`**（绕过 exports map，升级必炸）。
- 落入第 3/4 类时，长期正确动作是**推动上游导出**，而不是永久维护镜像。
- 镜像 SDK 内部实现的位置统一用 `SDK-MIRROR:` 标记注释（并写明对应的 SDK 源文件）；SDK 升级后用 `rg -n "SDK-MIRROR" src` 逐一比对。
- **资源面板的配色语法只有三态**，新增行不得引入第四种含义：常规前景色 = 已加载且生效；绿色 = **本会话中生效过**（技能加载、工具调用、提示词调用、扩展的命令/工具/事件钩子跑过、上下文文件随请求发给了模型）；灰 + 斜体（`inactive`）= 已配置但未生效。这里是**宽口径**：绿色回答「在这次对话里真的起作用了吗」，不是「用户看见它发生了吗」，所以多数扩展在 attach 后即绿、Context 在首轮请求后整栏转绿，属预期。绿色有两个来源并取并集——transcript 能看见的（`webview/resources-view.ts` 的 `mark*` 系列）与只有宿主能看见的（`ResourceItem.used`，见 `agent/activity.ts`）。不要再把“次要信息”用灰色表达，否则“未生效”就读不出来了。GUI 不列 `Themes`（插件只用 VS Code 主题变量，pi 主题在这里不生效）。**面板只列 pi 官方的资源类型**：单个扩展发明的目录约定（例如 `~/.pi/agent/agents/`）不进面板——pi 对它没有加载器，列出来等于把某个扩展的私有布局呈现成这个宿主的一等概念。
- protocol.ts 修改后需同步 host 端（bridge）与 webview 端（main.ts）两侧。
- UI 文案改动要同时更新 `i18n.ts` 的 en 与 zh 两套字典；宿主侧面向用户的文案（原生对话框、QuickPick、transcript 状态提示）全部放在 `src/shared/messages.ts`，通过 `agent/i18n.ts` 的 `t()` / `tf()` 取用，不要在代码里内联英文字面量或 `startsWith("zh")` 判断。有意不本地化的三类：`/` 命令目录（对齐 CLI）、spike 诊断命令（开发者工具）、SDK/模型产生的文本。**渲染方不同则字典不同**：上面两处都是插件自己画的 UI；而由 **VS Code 自己**渲染的清单文案（设置界面、命令面板、应用商店介绍）拿不到 `t()`，只能走清单本地化：`package.json` 里写 `%key%` 占位符，值放在根目录的 `package.nls.json`（英文兼兵底）与 `package.nls.zh-cn.json`（简体中文）；新增清单文案时两个文件都要补，不要在 `package.json` 里内联字面量。
- webview 端改动后 `pnpm verify` 会比对 `scripts/webview-snapshot.txt` 的 DOM 快照；差异即回归信号，确认无误后用 `node scripts/smoke_webview.mjs --update` 更新基线并在 commit 中说明。**单跑 smoke 前必须先 `pnpm build`**：它加载的是 `dist/webview.js`，不重建就是在测旧产物，会给出假的通过。

## 构建与验证

```powershell
pnpm build          # esbuild -> dist/extension.js + dist/webview.js
pnpm typecheck      # tsc --noEmit
pnpm verify         # 构建 + bundle 校验 + 无头冒烟测试
pnpm package:vsix   # 打包 vsix
```

改完代码至少跑 `pnpm build` 和 `pnpm typecheck`。涉及 SDK 加载/打包的改动跑 `pnpm verify`。

## 已知陷阱

- **子会话必须有自己的 `services`（否则毒死父会话的全部扩展）**：扩展是 per-`ResourceLoader` 加载一次的，`getExtensions()` 返回缓存结果，而 `core/agent-session.ts` 用它构造 `ExtensionRunner`、`core/extensions/runner.ts` 的 `bindCore()` 把**当前会话**的动作抄进那个共享 runtime（源码注释："all extension APIs reference this"）。于是共用一份 services 时：子会话一构造就把所有扩展的 `pi.*` 劫持到自己身上，`child.dispose()` 又经 `runner.invalidate()` 给共享 runtime 永久打上 stale 标记（并取消所有 `pi.events` 订阅），此后父会话里任何扩展调 `pi.*` 都抛 "This extension ctx is stale after session replacement or reload"，且**没有复位路径**（只有重建 services 才会重新加载扩展）。CLI 撞不到：`switchSession`/`newSession` 在 dispose 后立刻用工厂造全新 services。因此 `agent/subagent.ts` 的子会话走 `runtime.ts` 的 `createSubagentServices()`——只共享 `modelRuntime` / `settingsManager`（非会话绑定，且带着 auth 与 project-trust 决定），`resourceLoader` 一律新建。**并行时每一路都要各自一份**，不能 N 路共用。扩展 factory 会随之重跑一遍（provider 重复注册是 SDK 定义好的 merge 语义）；模块 factory 有进程级缓存，新 loader 首次 `reload()` 不清缓存，所以开销主要是重扫 skills/prompts/context（并行度上限的一部分理由）。`diagnostics.ts` 的 `subagent isolation` 自检项钉住它：跑完 `createSubagentServices()` + `child.dispose()` 后，父会话的探针扩展仍要能答出**父**会话的名字。

- **重载资源只能走 `AgentSession.reload()`**（同根的另一面）：会话的 `ExtensionRunner` 是构造时从 `resourceLoader.getExtensions()` 建一次的，而 `resourceLoader.reload()` 会产出**全新的** `extensionsResult`（新 runtime + 新实例）。所以“`loader.reload()` + `session.bindExtensions()`”比什么都不做还糟：会话继续用旧实例（新增/修改的扩展永不生效），`bindExtensions()` 还会向旧实例重发一次 `session_start`。`session.reload()` 才会拆旧 runner（shutdown + invalidate）、重载 settings/资源、重建 runner 与工具注册表（宿主 `customTools` 与激活集保留）并重发 `session_start`（reason `reload`）；SDK 三个 mode 的 `/reload` 全是它。它不换 session 对象，所以订阅与 histories 不受影响；但扩展实例换了，因此 `ChatBridge` 经 `beforeSessionStart` 钩子在“新 runner 已建、`session_start` 未发”的空档里清掉 setStatus/setWidget（新实例重新发布，同 `attach()` 的契约），并重建 skill/prompt 索引。`diagnostics.ts` 的 `extension reload` 自检项在两次读取之间改写探针扩展：新工具要出现、旧工具要消失，四个核心工具不能掉（`subagent` 开启时同理）。
- OAuth flow 需在入口静态注册（`registerBunOAuthFlows()`），bundler 无法跟踪 SDK 的动态 import。
- **`import.meta.url` 必须逐模块重建，不能全局指向入口**：打包成 CJS 后 `define` 把它改写成 `__piSdkEntryUrl`，对「定位包根」的用法（docs/examples/themes）没问题；但 `core/extensions/loader.js` 是拿它推**自己所在目录**来拼 jiti alias（`resolve(__dirname, "../..", "index.js")`），统一指向入口会高两层变成 `@earendil-works/index.js`，**所有 import 了 `@earendil-works/pi-coding-agent` 的扩展都会加载失败**（其余功能完全正常，所以很隐蔽）。`esbuild.mjs` 的 `sdkModuleUrlPlugin` 给每个 SDK 模块注入模块作用域的 `__piSdkEntryUrl`（遮蔽 banner 那个全局常量），`diagnostics.ts` 的 `extension sdk import` 自检项在 **bundle 内**加载一个 probe 扩展来钉住它。
- **`dist/node_modules/` 必须自给自足**：pi 扩展经 jiti 加载的是**磁盘上**那份 SDK（alias 锚在 SDK 入口的 `import.meta.url`），而不是 bundle 里那份，所以磁盘副本自己的裸 import（`partial-json`/`yaml`/`chalk`…）也必须随 VSIX 发出去。清单在 `scripts/runtime-packages.mjs`（**单一事实源**，esbuild 与校验共用），只收**静态加载路径的闭包**——provider SDK（anthropic/openai/genai/mistral/bedrock，约 35 MB）由 pi-ai 惰性 import，不入列。这个坑开发时看不见：从仓库跑时 Node 会向上找到项目自己的 `node_modules` 兜底，只有打包成 VSIX（vsce 剥掉根 `node_modules`）后才炸成 `Cannot find module`。`pnpm verify` 的 `scripts/check_extension_runtime.mjs` 在仓库外的沙箱里重建发布布局来钉住它。
- **清单包的版本按 SDK 的视角取，不是根 `node_modules` 的那份**（`runtimePackageSource()`）：hoisted 布局下，插件自己的直接依赖会拿下根位置，把 SDK 需要的版本挤到 `node_modules/@earendil-works/pi-coding-agent/node_modules/` 里。清单服务的是**磁盘上那份 SDK**，所以必须优先取它的嵌套副本；插件自己那份已经进了 bundle，永远不从磁盘读。真实例子：webview 用 highlight.js 11.x 做语法高亮，SDK 需要 10.x，而 11.x 的 exports 已不再开放 SDK 用的 `./lib/index.js`——复制根那份会让磁盘 SDK 直接加载失败（`ERR_PACKAGE_PATH_NOT_EXPORTED`）。**新增与 SDK 同名的直接依赖时先想这一层。**
- undici 是本仓库的**显式直接依赖**（`src/agent/http.ts` 装全局代理 dispatcher）；esbuild `alias` 把所有 `import "undici"`（含 SDK 嵌套副本）收敛到这一份，单副本 + >= 8.7.0（代理转发修复）由 `scripts/check_bundle.py` 断言；升级时保持 >= SDK 自带版本。
- webview CSP 严格：脚本必须带 nonce，样式/图片只允许 `webview.cspSource`。
