# AGENTS.md — pi-agent-chat

非官方 VS Code 插件：在侧边栏与编辑区标签页提供 Pi Coding Agent 的聊天 UI。agent 能力来自官方 SDK `@earendil-works/pi-coding-agent`（不走 RPC，不要求用户安装 pi CLI）。

**插件是 pi 的另一个宿主，不是 CLI 的前端。** 选了 SDK 而非 RPC，就意味着宿主环境不同（扩展宿主 vs 终端进程），「与 CLI 能力面完全一致」从来不是可达目标——CLI 生态里任何依赖终端宿主的扩展在这里都会失效。因此兼容边界划在**数据面**而非能力面，见下面第一条红线。

## 架构速览

- `src/extension.ts` — 插件入口：注册侧边栏 webview view（`piAgentChat.view`）、编辑区 `WebviewPanel` serializer、命令与诊断。
- `src/chat-surfaces.ts` — 顶层聊天编排：sidebar/editor 是可替换 GUI surface，每个独立会话由自己的 `ChatController`（`PiRuntime` + `ChatBridge`）驱动；窗口级 session claim、surface 间移动、关闭 editor 后无面保活到 settle、peer 会话状态都在这里。
- `src/agent/runtime.ts` — SDK `AgentSessionRuntime` 薄封装；负责 session 新建/切换/fork 与 extension 重绑定；扩展 UI 的 `ctx.ui.notify` 经 sink 转到 transcript。
- `src/agent/bridge.ts` — 双向翻译层：SDK 事件 → `HostMessage`，webview 消息 → runtime 操作；session 历史回放。
- `src/agent/auth.ts` — 登录/登出流程：`ModelRuntime.login()` + `AuthInteraction` 映射到 VS Code 原生对话框。
- `src/agent/commands.ts` — 斜杠命令目录与内置命令分发（命名对齐 CLI）。
- `src/agent/model-picker.ts` — 两层模型选择：`buildModelCatalog()` 供 composer 快捷菜单列出范围内模型，`pickModel()` 为完整原生 QuickPick（搜索、能力详情、⭐常用、📌默认），另有 `/scoped-models` 批量管理；常用模型写共享设置 `enabledModels`。
- `src/agent/model-config.ts` — 自定义供应商：打开共享的 `~/.pi/agent/models.json`（空文件写入带注释的 JSONC 模板，已有内容则插入一条新模板）、列出其中定义的 provider、以 `jsonc-parser` 删除单个 provider 条目；供应商列表首行的入口 + bridge 的保存监听共同构成「编辑即生效」。
- `src/agent/skills.ts` — 技能路径索引：把 `read`/`bash` 调用判定为「加载技能」或「技能资源」，供 transcript 与资源面板区分展示
- `src/agent/tool-details.ts` — 工具 `AgentToolResult.details` 跨界到 webview 前的清洗（排除清单 + 深度/条目/字符预算截断 + 不可克隆值剔除）。
- `src/agent/activity.ts` — 宿主侧「本会话中真正生效过」的判定，专供资源面板点亮 Context / Extensions 两栏（transcript 里看不到这两类活动）：上下文文件每轮都无条件拼进 system prompt，故按「本会话已发出过请求」判定；扩展按它订阅的事件（`Extension.handlers` 的 key）是否已被 emit 推断，外加 `onError` 直证。
- `src/agent/invocations.ts` — 提示词模板与扩展命令的归属判定：`prompt()` 会把 `/模板` 展开成正文、把扩展命令直接消费掉，所以归属只能在提交前从原文解析（回放时仅能靠无占位符的模板正文精确匹配），供资源面板点亮对应行。
- `src/agent/subagent.ts` — 插件在 pi 自带工具之外提供的两个工具之一：`subagent`，并行子会话调度与逐路汇报（默认关闭，见红线）。
- `src/agent/scope.ts` — 子代理写入范围：路径前缀规范化、启动前重叠判定、`ScopeGuard`（越界拒绝 + 写入记账）。
- `src/agent/scoped-tools.ts` — 用 SDK 导出的工厂重建的同名 `edit`/`write`，仅替换文件操作层以实施范围强制（唯一强制点）。
- `src/agent/vscode-terminal.ts` — 另一个自有工具 `vscode_terminal`：在用户可见、可键入的集成终端里跑命令（默认关闭，见红线）。终端池 + `run`/`list`/`read`/`close` 四个动作；终端 API 经 `TerminalApi` 注入，自检可用脚本化实现驱动。
- `src/agent/terminal-replay.ts` — 迷你 VT 重放（零依赖）：遵从光标指令而不是剥离它们，返回屏幕文本 + 光标行；光标行是 `read` 增量续读的边界。附用例，spike 与 `pnpm verify` 共用同一份。
- `src/agent/terminal-spike.ts` — 终端能力的真机探针（命令面板触发）：只有真窗口 + 真 shell + 真人能验证的事实（激活耗时、用户键入可捕获、增量交付、退出码保真）。保留不废弃：它是这个功能唯一的真机验证手段。
- `src/agent/config.ts` — 插件自有能力的 VS Code 配置（宿主侧唯一读取点，workspace 级可覆盖），并提供打开 VS Code 设置界面用的 section id。
- `src/agent/resume.ts` — 自动重试放弃后的手动重发（「retry failed」通知上的重试按钮）。
- `src/shared/protocol.ts` — host ↔ webview 消息协议与共享常量，**必须保持零依赖**（webview 打包不能引入 Node 代码）。
- `src/shared/messages.ts` — 宿主侧文案的中英字典（含参数化模板），同样零依赖；宿主经 `agent/i18n.ts` 的 `t()`/`tf()` 取用，webview `i18n.ts` 也引用它以保持措辞一致。
- `src/webview/` — webview 前端（无框架，DOM 直操作），按面板拆分：`main.ts` 只做布局/接线/路由，`transcript.ts`、`composer.ts`、`sessions-view.ts`、`resources-view.ts`、`picker.ts`、`statusline.ts`、`search.ts`（transcript 搜索，高亮走 CSS Custom Highlight API，不改 DOM）各管一块，`shell.ts`/`store.ts`/`host.ts`/`collapsible.ts`/`bubble.ts`/`clipboard.ts`/`dom.ts` 为公共设施；`i18n.ts` 提供 zh/en 双语。
- `src/styles/` — SCSS 样式源（`main.scss` 入口，经 esbuild 编译到 `dist/main.css`），颜色只用 VS Code 主题变量。

项目模块架构见 `ARCHITECTURE.md`；修改模块结构后请更新该文件。

## 关键约定

- `package.json` 的 `dependencies` 与 `devDependencies` 必须使用固定版本号，禁止 `^`、`~` 等版本范围；新增依赖使用 `pnpm add --save-exact`（`.npmrc` 已默认启用 `save-exact=true`）。`engines` 等兼容性声明不受此限制。提交前用 `pnpm check:dependencies` 检查。
- **兼容边界在数据面（红线）**：会话文件与配置完全复用 `~/.pi/agent/`（auth.json、models.json、settings、extensions、skills、agents），两个宿主随时能接手对方的会话；不引入插件私有的配置副本。插件**允许**拥有 CLI 没有的能力，但受两条约束：
  1. **插件独有工具的名字由插件决定，同名扩展工具按名字屏蔽**。插件不知道某个扩展注册的同名工具是怎么实现的、依赖哪个宿主的什么能力，所以一个名字在本窗口只能有一个含义：插件选的名字（例如 `subagent`）要么解析到插件自己的工具，要么（开关关闭时）根本不存在，**任何情况下都不会解析到扩展的同名实现**。这是对生态的**尽力兼容**而非完美兼容：对方宿主 resume 后，模型若模仿历史里的调用形式，可能对那边同名不同义的工具报参数错误——会话多半在一个宿主上跑完、跨宿主 resume 罕见，产品定位（SDK 宿主）下接受这个成本，不做更重的保证。
  2. **会话文件必须保持 CLI 可读可继续**。标准不是「CLI 能完整复现这个会话的能力」，而是**「CLI 能打开、能读懂、能接着往下做，只是不能再调用那个独有工具」**——且不承诺跨版本可读（内置 SDK 与用户 CLI 的版本漂移是上游格式演进的责任，插件不参与）。实质是**插件不成为第三个会话格式作者**：只写 SDK 写的东西，不加私有字段。推论：独有工具的**结果文本必须自解释**（脱离工具语境也能读懂发生了什么），不能只把信息放在结构化 `details` 里。
- **顶层多会话与 GUI surface（红线）**：侧边栏 `WebviewView` 与**任意多个**编辑区 `WebviewPanel` 是完全等价、可替换的呈现层；agent 的生命周期属于扩展宿主里的 `ChatController`，**不能绑定到 webview 生命周期**。用户看到的是**三个区域**：A 辅助侧边栏 / B 编辑区 / C 新窗口。B 与 C 在 API 层是同一种东西（`WebviewPanel` 不带窗口身份，`window.tabGroups` 只读且不告诉你 tab 在哪个窗口），所以「它在哪个区域」是 `chat-surfaces.ts` 自己的记账（`PanelRegion`），以 `viewColumn === One ⟹ 编辑区`（浮动窗口的组永远拿不到第 1 列）校正用户手动拖拽；记账失准只影响菜单措辞，两个动作都仍可用。**每个区域的 `…` 菜单 = 移动到另外两个区域 + 在两个多实例区域（B / C）新开会话**，固定 4 项。**没有「在侧边栏新开」这一项**：A 只能装一个会话，在它「新开」实质是**覆盖**（把当前会话顶掉 / 转后台），而从 B / C 发起时发起者往往**看不见侧边栏装着什么**（它甚至可能是关着的）——对看不见的面做替换，与 B / C 新开（永远是加一个 tab / 窗口，不动任何现有东西）不是一类动作；A 的新会话归 header 那个按钮，由看得见它当前内容的人发起。新开项固定按 B→C 排序。能力以原生 API 为准，做不到的项直接不出现（不做垫片）。切换显示位置只能走 VS Code 原生标题菜单：sidebar 由 `contributes.submenus` 在 `view/title` 声明自己的 `…` icon；editor 的菜单项必须以非 `navigation` group 并入编辑器自带的「更多操作」菜单，不能再增加第二个 `…` icon；两套 `when` 用 `piAgentChat.chatTabRegion` 区分 B / C。**移动到 B、以及在 B / C 新开会话，一律开新 tab / 新窗口**；只有 A 是覆盖语义（侧边栏没有 tab）。三条移动路径各有其唯一手段，且都经真机探针验证：A→C / B→C / 在 C 新开都走 `workbench.action.moveEditorToNewWindow`，而它**只作用于活动编辑器且不接参数**，所以：**去往新窗口的载体 panel 必须建在 `ViewColumn.Active`**（用户此刻所在的那个窗口），发命令前还要等到 `panel.active`，等不到就**放弃移动**（宁可留成 tab，不能把别人的编辑器甩到新窗口）。把载体建在主窗口再想从浮动窗口把焦点抢回来是不可靠的（`switchToMainWindow` + `reveal` 实测仍拿不到激活，新会话静默地留在原地）；顺着焦点建则它天生就是活动编辑器。反过来，**「在编辑区新开」必须显式 `ViewColumn.One`**——用 `Active` 会在浮动窗口聚焦时把它开进那个浮动窗口。**C→B 走 `panel.reveal(ViewColumn.One)`**——VS Code **没有** `moveEditorToMainWindow` 命令，`tabGroups` 也没有 move API，而 `restoreEditorsToMainWindow` 会收回所有浮动窗口的编辑器（太粗，不用）；B/C→A 把 controller 交给 sidebar 后**必须关闭来源 tab**。命令作用于哪个 tab **只能实时读 `WebviewPanel.active`**（全窗口至多一个为真，且与 `activeWebviewPanelId` 同源；缓存「最后一次活动」会被其他窗口的 view-state 事件覆盖成错的 tab，已实测踩过）。header 固定保留「新会话 / 会话列表 / 会话树 / 搜索 / 资源 / 设置」六个文字按钮，不再另造 surface 切换入口。关闭 tab 只拆 GUI，运行中的 controller 必须无面继续，直到 `agent_settled` / compaction 结束后才释放，历史由 SDK 正常落盘；**tab 的注销必须按集合查找而不是与某个单值字段比较**（曾经用 `this.editor !== surface` 早退，多开一个窗口后旧 panel 就永远不会 detach，claim 一直不释放）。「新会话」与会话列表中选择另一个未占用会话都只作用于发起操作的 surface：若当前 controller 正在运行，就把它转为 background 保活，并让新建或选中的会话通过新 controller 接管同一 surface，不得中止旧运行或把新会话送到另一面；普通顶层会话不再为了避开运行中 controller 而降级成只读 replay。一个 session file 在本窗口只能被一个顶层 controller claim（包括无面的运行中 controller），resume、历史子代理 replay、扩展 `ctx.switchSession()` 都必须在替换前检查；会话列表中被可见 controller 占用的行显示「已在另一处打开」并仍可点击：点击后把该 controller 移入请求方 surface，来源 surface 立即由一个空的新会话接管；被 background controller 占用的行单独显示「后台运行中」，点击后同样把该 controller 移入请求方 surface。两种情况均不得构造第二个 writer；请求方原 controller 若运行中则转为 background 保活，否则释放。并存的顶层 runtime **绝不共享 `ResourceLoader` / extension runtime**（会触发同「子会话毒死父会话」的 stale 问题），只可经 `createIsolatedServices()` 共享 `modelRuntime` / `settingsManager`。多个顶层 agent 允许同时在同一 cwd 直接工作：插件不建 worktree、不串行、不检测写冲突，也不弹确认框；用户自行判断并发任务并由 git 兜底。子代理 lane 不计入顶层多会话；「用户只跟主代理对话」仍指**每个顶层会话内部**只有 parent 可输入，不禁止两个独立顶层会话。「新会话」与会话列表中选择另一个未占用会话都只作用于发起操作的 surface：若当前 controller 正在运行，就把它转为 background 保活，并让新建或选中的会话通过新 controller 接管同一 surface，不得中止旧运行或把新会话送到另一面；普通顶层会话不再为了避开运行中 controller 而降级成只读 replay。一个 session file 在本窗口只能被一个顶层 controller claim（包括无面的运行中 controller），resume、历史子代理 replay、扩展 `ctx.switchSession()` 都必须在替换前检查；会话列表中被可见 controller 占用的行显示「已在另一处打开」并仍可点击：点击后把该 controller 移入请求方 surface，来源 surface 立即由一个空的新会话接管；被 background controller 占用的行单独显示「后台运行中」，点击后同样把该 controller 移入请求方 surface。两种情况均不得构造第二个 writer；请求方原 controller 若运行中则转为 background 保活，否则释放。并存的顶层 runtime **绝不共享 `ResourceLoader` / extension runtime**（会触发同「子会话毒死父会话」的 stale 问题），只可经 `createIsolatedServices()` 共享 `modelRuntime` / `settingsManager`。多个顶层 agent 允许同时在同一 cwd 直接工作：插件不建 worktree、不串行、不检测写冲突，也不弹确认框；用户自行判断并发任务并由 git 兜底。子代理 lane 不计入顶层多会话；「用户只跟主代理对话」仍指**每个顶层会话内部**只有 parent 可输入，不禁止两个独立顶层会话。
- **配置项归属（红线）**：**插件独有的能力 → 插件自己的 VS Code 配置；与 CLI 共享的能力 → 共享配置 `~/.pi/agent/`**。两个方向都不能跨：把独有能力的开关塞进共享 settings 是污染（CLI 会读到看不懂的键）；把共享能力分叉到 VS Code 配置会让两个宿主行为不一致，比没有这个功能更糟。模型、认证、扩展、技能、常用模型（`enabledModels`，语义对齐 CLI `/scoped-models`：显式 `provider/modelId` 列表，全选或空则清空）均属后者。VS Code 配置另有一个共享 settings 给不了的好处：**workspace 级覆盖**（同一用户可以在实验项目开启、在生产仓库关闭）。
- **工具集（红线）**：插件只在 pi 默认的 `read`/`bash`/`edit`/`write` 之外注册两个工具——`subagent` 与 `vscode_terminal`（两者都默认关闭，见下两条），且**不调用 `setActiveToolsByName()`**（自定义工具在 `_buildRuntime({ includeAllExtensionTools: true })` 时已自动激活，手动重写激活集只会静默关掉扩展工具）。`grep`/`find`/`ls` 等 SDK registry 里的其他工具，一律由 `~/.pi/agent/extensions/` 下的 pi 扩展提供（CLI 与 GUI 共享同一份）。**再加下一个工具的门槛（三条全部满足才行）**：① **语义可在其他宿主重现**（见上面「VS Code 能力的使用边界」）；② **能用这个宿主的结构性优势立论**——CLI 宿主结构上给不了，而不是「用 VS Code API 能做」，也不是「在这里做更快」；**能用 pi 扩展实现的能力一律不满足本条**（扩展能 spawn 任何进程，且 CLI 与 GUI 共享同一份，插件再造一份只会产生行为漂移）；③ 满足互通红线的两条约束（若与扩展同名则该扩展工具被屏蔽、结果文本自解释）。图方便就加工具，会同时破坏会话互通和用户对两个入口的预期。
- **`subagent` 工具（红线）**：插件自有工具之一，能力是**可写的并行委派**——主代理一次派出多路子会话，各自在声明的路径范围内直接改工作区。它的立论是这个宿主的结构性优势（in-process 子会话共享认证与模型运行时、完整事件流可订阅、GUI 能把 N 路空间化呈现），CLI 宿主只能 spawn 独立进程 + `--no-session`，结构上给不了。名字就用生态里耳熟的 `subagent`（模型一看就知道是委派），因此同名扩展工具被屏蔽（见下）。以下设计决定改动前先读完：
  - **默认关闭**，由 VS Code 配置开启（激进功能，用户自主选择；workspace 级可覆盖）。另有两项：**最大并行数**（默认 3，硬顶 8）与**子代理默认模型**（留空则继承父会话）。默认 3 是三条曲线的交点（明显快过串行 / 人还跟得过来 / 写冲突概率尚低），硬顶 8 是在保护功能定义——N 大到无法观察时它就不再是「可观察的并行」。**生效时机是「下一次会话构造」而不是「下一个会话」**：工具集在会话构造时固定，`session.reload()` 保留宿主的 `customTools`，但 SDK 的 `AgentSessionRuntime` 在 new / switch / fork / import 与窗口启动时都会重跑工厂（`createRuntime` 里重读 `readSubagentConfig()`），所以**切走再切回**同一个会话也会带着新配置重建，历史不丢。`ChatBridge` 监听配置变更（防抖 300ms，设置界面是逐键写入的）并按会话状态分两种反应：**空会话直接重建**（`applySubagentConfigChange()` → `newSession()` + `attach()`），非空会话只发提示。空会话是唯一没有出口的状态——header 的「新建」按钮在空会话上本来就是禁用的，不重建就只剩重载窗口一条路；而它也是唯一重建零损失的状态（未落盘的空会话在磁盘上根本不存在，被命名过的空会话文件仍留在列表里），所以「不能在用户背后扔掉对话」这条顾虑在这里不成立。**比较对象是会话构造时那份配置**（`PiRuntime.builtSubagentConfig`）而不是上一次读到的值：重复保存同样的值不该付出一次重建。**三项值没有插件自己的表单**：header 设置菜单只留「插件设置」一个入口，直接把 VS Code 设置界面定位到 `piAgentChat`（`workbench.action.openSettings`）。曾经另有「子代理」「终端工具」两个更窄的入口，已删：它们只是同一个设置界面多一层过滤，而设置界面自带搜索。曾经还做过一个 QuickPick 表单（先选写入作用域再逐项改），同样已删：它把一个单页面拆成了三层嵌套点击，而它能提供的东西（作用域、当前值、说明文字）设置界面本来就每样都有且更好。推论：描述文字归 `package.nls*.json`（设置界面由 VS Code 渲染，拿不到 `t()`），插件不再为这三项维护任何写入路径。
  - **新会话提示必须区分开关两态**：屏蔽扩展 `subagent` 的那段提示（见下）在开关关闭时不能说「改用本窗口的子代理」——那时会话里一个委派工具都没有，得直说并指路设置菜单。为此 `history` 消息携带 `subagent: { enabled, shadowedExtension? }`，且 `enabled` 取自**会话构造时**记下的 `ToolSetupRef`（不是当下的配置值），因为提示描述的是屏幕上这个会话怎么装配的。功能默认关闭且无扩展被屏蔽时不可见，故同一段落另有一条独立的「功能未开启」提示（无扩展语境，只指路「设置 → 插件设置」），也属于 `emptySession()` 的条件段落；开启且无屏蔽时不提示（用户自己开的，资源面板里也看得见）。
  - **约束分三层，插件只做前两层**：机制层（schema 字段、并行上限、scope 重叠检查、越界写拒绝）是硬约束，由插件强制；事实层是工具 `description`，只陈述机制；策略层（何时该用、怎么切分、本项目允不允许）属于用户的 `APPEND_SYSTEM.md` / 项目 `AGENTS.md`，插件一个字都不写。**用户的提示词管不了机制**，所以可写并行的风险只能靠机制兜，不能靠劝告。
  - **上限值即 `maxItems`**：配置项的值直接生成到 schema 的数组上限里，一处配置同时是文档（模型事前可见，不会提交超额再被拒，省一轮往返）与强制（超了是结构化报错，模型能自我修正）。不要在 prompt 里重复一遍。
  - **scope 必填且必须机制化执行**：每路子任务必须声明可写路径范围。启动前静态检查各路是否重叠，重叠即拒绝启动；运行中子代理越界的 `edit`/`write` 直接拒绝。范围只是说明性字段而不强制，等于退化成 prompt，整个设计作废。让模型填字段本身就是逼它把「这两个任务真的独立吗」显式化，比任何忠告都有效。告知子代理自己的 scope 不算注入提示词：它描述本次运行的配置、作者是主代理（通过工具参数指定）、且有机制强制，同告知 cwd 是什么。
  - **scope 用路径前缀而不是 glob**（`src/agent/scope.ts`）：重叠检查必须是**可判定**的，而两个任意 glob 是否相交一般情况下不可判定——glob 形状的 API 会把「证明不重叠」惄惄降级成「猜不重叠」，而那正是 scope 要消除的东西。前缀同时让包含判定和重叠判定都变成字符串比较。
  - **强制点在 operations 层**（`src/agent/scoped-tools.ts`）：`CreateAgentSessionFromServicesOptions` 没有 `toolsOptions`，内置工具无法重配，所以子会话用 `excludeTools: ["edit","write"]` 排掉它们，再经 `customTools` 注入用 SDK 导出的 `createEditToolDefinition` / `createWriteToolDefinition` 建的**同名同 schema 同行为**版本，只替换 `EditOperations` / `WriteOperations`。不复刻任何 SDK 逻辑；记账也在这一层顺手完成。**读不受限**（子代理必须能理解它改不了的代码）。**推论：不得引入第二条写路径**——任何绕过 `edit`/`write` 的写入工具（比如某个基于 `workspace.applyEdit` 的 rename）都会击穿 ScopeGuard，「唯一强制点」就不再唯一，scope 的整个设计价值随之作废。
  - **已知缺口：`bash` 写文件无法拦截也无法记账**。shell 命令写不写、写哪里不跑就不可判定（`BashToolOptions.spawnHook` 只能改 command/cwd/env）。汇报里**必须明写这一点**，不能让模型以为文件清单是完整的。
  - **直接写工作区，不做隔离副本**：环境真实，构建与测试结果可信，行为与单代理一致。代价是失败时留下半成品，由 git 兜底（非 git 工作区是本方案的真实风险敞口）。
  - **失败照实汇报，不自动回滚，但必须逐路记账**：结果要写明每路的状态、scope、失败原因、**实际写过哪些文件**，以及失败路是否留下了半成品（这一项要显式标注，模型读到「失败」很容易默认「什么都没发生」）。**失败原因只列能机械判定的**（`error` / `stopped_by_user` / `stopped_with_run`）：「子代理主动放弃」除了措辞之外与正常结束无法区分，猜一个就是向主代理编造事实；越界次数作为**独立字段**无论成败都汇报（它是切分质量的信号），**并且必须连同被拒的路径一起给**（`ScopeGuard.deniedPaths`，去重 + 有上限）。理由：**有些改动天生跨切面**（重命名函数 + 改调用方），任何对目录树的划分都关不住它，而光给个计数等于让主代理猜「差在哪里」；给了路径，拒绝就从「挡住」变成「情报」，主代理能自己收尾或重新切分。**不引入文件锁来“修复”这个场景**（已评估并否决）：锁给的是互斥，而跨切面改动缺的是**原子性**（不变量跨文件，锁的粒度在文件）；又因为没有回滚，拿不到锁时阻塞会死锁且不能安全中断、失败则直接留下半个重构，等于把冲突检测从「开跑前、免费」挪到「跑到一半、不可撤销」。真要并行做跨切面改动，对的方向是隔离（git worktree + 合并）而不是互斥。记账不是回滚：回滚被否决了，记账是「交给模型判断」能成立的前提，否则就是让模型盲猜。
  - **不引入子代理角色文件（已评估并否决）**：曾复用 `~/.pi/agent/agents/*.md`（CLI subagent 扩展的角色定义），后按本条删除。理由：那份约定**不是 pi 的**——SDK 里没有任何 agents 加载器（skills / prompts / extensions / context 都有），目录、frontmatter 字段与 `tools`/`model` 语义全由那个扩展自己定义。消费它等于把某个第三方扩展的私有布局固化进插件的能力面，而插件同时还**屏蔽**着它的工具：一边声明「不看它怎么实现」、一边吃它的数据格式，前后矛盾；用户没装那个扩展时，该目录对 pi 而言更是毫无意义。角色提供的三样东西各归其位：systemPrompt 是**行为引导**，属于用户提示词（由主代理写进 `task`）；`model` 已是 schema 字段；`tools` 白名单是**机制**，要恢复就做成 schema 字段（宿主无关），不能靠外部目录约定。**推论：资源面板只列 pi 官方的资源类型。**
  - **模型解析按来源分两种失败方式**（`planModel()`，优先级：工具参数 > 子代理默认模型设置 > 父会话模型）。**主代理自己填的 `model` 找不到 = 硬错误**，且在任何一路启动前抛出：那是机械可判定的参数错误，告诉它才能自我修正。**用户配置的那一级（插件设置）找不到则降级**到父会话模型，并且**只提示用户**（`LaneNotice` → `ChatBridge.onLaneNotice()` → 主会话 transcript 的 status 卡片），主代理的汇报里一个字都不写。理由：那个模型不是主代理选的，它也改不了拼写或去登录供应商，报给它只会让它去「修正」自己没发过的参数；而整路任务因为一个笔误全废，代价远大于换个模型跑完。`diagnostics.ts` 的 `subagent model selection` 自检项钉住这两个方向。
  - **用户只跟主代理对话**：子会话面板只读、无输入框；对子代理只有「看」和「停某一路」两个动作（都是对**运行**的控制，不是对话）。**对话线永远只有一条**，这是并行功能最大认知风险（N 个 agent 在跑，用户该跟谁说话）的正解。推论：启动并行后视图**停留在主会话**（串行版那种自动跳转在并行下无解），并行卡片就画在主会话 transcript 的工具卡片里（不引入新导航层级，且跑完后天然留在历史里可回看），点击某一路才进入其只读预览；
  - **实时进展行是命题成败所在**：主代理等待期间自己没有输出，主会话的全部活跃感来自那张卡片。每路必须持续显示「此刻在做什么」（当前工具调用 / 刚改的文件），都只写「运行中」等于看起来卡死，「可观察的并行」就成了空话。为此：卡片**默认展开**（`startToolCard` 里对这一个工具名特例，且必须在 `entry` 赋值后才 `setExpanded(true)`，render 回调会反读 `entry`）；`tool_update` 事件**带 `details`**（原本只有 `text`，不带的话卡片在整个运行期间都是静止的）；`subagent` 也因此**不在** `TOOLS_WITH_DEDICATED_CARDS` 里——它的卡片恰恰是用 `details` 画的。
  - **用户正停在某一路时跑完了不要自动弹回**，只在返回入口标记「主代理有新进展」（他是主动点进去的，弹走会丢失阅读位置）。停单路的汇报必须写明「用户主动中止」，否则主代理会好心重开一路，把用户刚踩的刹车松掉。
- **`vscode_terminal` 工具（红线）**：插件自有工具之二，能力是**在用户看得见、也能直接键入的集成终端里执行命令**，且用户键入的内容进入返回给模型的 transcript。立论在于三件事的**合取**：用户能直接敲 + agent 拿到完整 transcript + 聊天界面不消失。SDK 自带的 `examples/extensions/interactive-shell.ts` 用 `tui.stop()` + `stdio:"inherit"` 能做到第一件，代价是输出直接进 tty、agent 什么都拿不到（那个例子只能返回 `"(interactive command completed successfully)"`）；CLI 宿主要同时满足三件，只能自己写一个终端模拟器塞进 TUI。名字用 `vscode_terminal` 而不是 `terminal`：碰撞概率低（同名扩展工具必须被屏蔽，就该选低碰撞的名字），且跨宿主 resume 时名字本身就在做自解释。以下设计决定改动前先读完：
  - **默认关闭**，由 VS Code 配置开启（`piAgentChat.terminal.enabled`），另一项是**同时打开的终端数**（默认 3，硬顶 8，`clampTerminalLimit()`；清单的 `maximum` 只约束设置界面，手改 settings.json 能填任何数）。**不用 `0` 兼任关闭**（可复用的规则）：布尔管「能力存不存在」，数值管「已存在的能力怎么行为」；合并会让「关掉再打开」丢掉调好的值、使 `minimum: 1` 自相矛盾，且 `0` 在很多工具里是「无限制」，用户按那个直觉填 0 会让功能静默消失。（`transcript.foldLines: 0 = 永不折叠` 不是反例：折叠不是可以不存在的能力，0 只是把阈值推到极限。）生效时机、空会话重建与新会话提示均与 `subagent` 同机制（`applyTerminalConfigChange()` 与 `applySubagentConfigChange()` 共用 `applyToolConfigChange()`，比较对象是 `PiRuntime.builtTerminalConfig`）。
  - **无 shell integration 就不执行**（抛错，不是返回空成功）：拿不到输出的命令执行器就是扩展式 `subagent` 在本宿主的那个失败模式（exit 0 + 空输出，模型拿着空结果继续推理）。为此创建的终端要随手收掉。
  - **退出码只能报真的**：VS Code 的 `shellIntegration.ps1` 在 OSC 633 `D` 里发的是 `[int]!$?`（它自己的变量名就叫 `$FakeCode`），而 `shellIntegration-bash.sh` 发的是真 `$?`。该脚本随产品发布、不属于 API，抬 `engines` 改变不了它。所以 pwsh 下只写「成功/失败」，**不得把编造的 `1` 当退出码交给模型**（退出码有语义：`grep` 1=无匹配/2=出错，`diff` 1=有差异/2=出错），同 `subagent` 那条「失败原因只列能机械判定的」。POSIX shell 照常报真值——两个 shell 行为不同是诚实，不是不一致。也**不做变通**（往命令后追加 `; Write-Output "__pi_exit=$LASTEXITCODE"`）：那会让用户看见的命令 ≠ agent 请求执行的命令，而「用户能看见 agent 在跑什么」正是本工具一半的立论；且 `$LASTEXITCODE` 只由原生命令设置，cmdlet 失败时是陈旧值。
  - **四个动作互相咬合，缺一不可**：超时不能 kill（命令可能正等用户输入）→ 必须有「还在跑」返回态 → 必须能事后查状态与取回输出（`read`）→ 卡死的终端会把后续 `run` 堆死，必须有出口（`close`）→ 要能指定对象就得能列举（`list`）。
  - **超时是 schema 字段而不是配置**（`timeoutSeconds`，默认 30、硬顶 300）：模型比全局设置更清楚 `npm install` 与 `git status` 各该等多久，形状也与内置 `bash` 的 `timeout` 一致；硬顶保证 agent 不会永久停摆。超时**绝不 kill**，只返回已产生的输出并说明它仍在跑。
  - **终端 ≠ 一次执行**：OSC 633 的 `A`/`B`/`C`/`D;<code>` 把同一终端里的每条命令切开，所以命令结束即返回工具结果、终端保持存活供下次复用（新建 ≈ 4.3s，复用 ≈ 15ms，所以**必须复用**）。注意 `C` 与 `D` 之间**没有任何分隔符**区分「程序输出」与「终端对用户按键的回显」，通用情况下拆不开。**不做预热**（启动时预先创建终端）：用户没要求就冒出一个终端比付一次 4 秒更差，而这个代价已写进 description。
  - **续读按行号且以光标行为边界**（`terminal-replay.ts` 返回 `cursorLine`）：重放总是对**整段**原始流做（只重放尾巴会丢掉光标指令所参照的屏幕上下文），光标行之前的行计入已读，光标行本身每次重发。否则进度条重绘同一行会要么被当成不断的新行，要么因「行数变短」触发全量重发。
  - **给模型的文本必须是 VT 重放结果，不得用正则剥离转义**：剥离丢掉的是「覆盖」指令而不是被覆盖的字符，会产生幻影文本（spike 实测：一次退格就出现 `pi-typed-qf8j0ggg  ^V   g`）。重放是本功能唯一有实际逻辑、且会**静默**回归的部分，因此用例与实现同居一个零依赖模块，同时被 spike 与 `scripts/check_terminal_replay.mjs`（接在 `pnpm verify`）跑。全屏 TUI（vim/htop）不在范围内，这是 **description 里陈述的机制事实**，不写成行为劝告。
  - **只能关自己创建的终端，且绝不自动关闭**：用户手开的、其他扩展的终端既不进 `list` 也关不掉（机制强制，不是提示词）；只有模型显式调 `close` 或用户自己点 × 才关。agent「任务完成顺手清理」把用户正在看、可能正在打字的终端关掉，与「用户正停在某一路时不要自动弹回」是同一类错误。同理，`PiRuntime.dispose()` 只退订阅不关终端。
  - **子代理拿不到这个工具**（`NEVER_IN_CHILD`）：终端是**共享且可见**的表面，前面只坐着一个人；N 路子代理往同一批终端里写（或各开一个）会交织成没人能跟的东西，而且用户无从得知是哪一路在问。子代理仍有 `bash`（不需要观众）。
  - **输出预算与 `bash` 同级**（SDK 导出的 `truncateTail` + `DEFAULT_MAX_LINES`/`DEFAULT_MAX_BYTES`）：终端 transcript 比纯 stdout 只大不小（多了提示符、命令回显、进度条重绘），截断后必须写明截了多少。
  - **终端 API 经 `TerminalApi` 注入**：`diagnostics.ts` 的 `terminal tool behaviour` 自检项用脚本化实现驱动拒绝路径（无 shell integration、超时不 kill、增量读、`close` 不碰别人的终端）——真终端只能靠运气走到这些分支。事件订阅**懒初始化**（首次创建终端时才订）：池对象在每个会话都会构造，包括 `vscode` 被桩揉掉的无头自检。
  - **能力面与 `bash` 重叠，已知情接受**：跨宿主 resume 时模型可能照着历史调一个 CLI 里不存在的工具，而跑命令是最高频调用。默认关闭 + 用户自选 + `vscode_` 前缀让模型更容易识别为宿主专有，是这个成本的全部缓解。长期运行的进程归 `services` 扩展，description **一个字都不提**（提了就是行为引导），靠机制自然分流。
- **始终屏蔽扩展注册的同名工具（`subagent` / `vscode_terminal`），与开关无关**：屏蔽理由是**同名屏蔽**——本窗口自带这两个工具，这两个名字归插件所有；与扩展的实现、它依赖哪个宿主的能力无关（插件既不认扩展身份，也不看它怎么实现）。两种开关态由机制各自保证：开关**开**时不排除、靠 SDK 工具注册表的覆盖语义赢（custom tool 在 `_refreshToolRegistry()` 中后写覆盖同名扩展工具，模型解析该名字永远拿到本窗口的工具）；开关**关**时经 `excludeTools` 把名字整个排除。排除集与 custom tools 都持久化在会话上，`/reload` 后同样成立。附带事实：扩展式 subagent 实现在本宿主本来就会静默失败（`ExtensionContext` 没有任何开子 session 的能力，扩展只能重新启动 pi，靠 `process.argv[1]` / `process.execPath` 自省定位——在扩展宿主里那指向 VS Code 自己的 `bootstrap-fork.js`（**它存在**，所以 `existsSync` 通过），结果是 spawn `Code.exe` 后 **exitCode 0 + 空输出**，模型拿着空结果继续推理），所以屏蔽不丢任何能力。状态很干净：开关关 → 这个名字不存在；开关开 → 只有本窗口的实现。**只按工具名，不认扩展身份、不看它怎么实现**（`findShadowedExtensionTool(services, name)` 就只接一个名字）；被屏蔽者经 `PiRuntime.shadowedSubagentExtension` / `shadowedTerminalExtension` → `history` 消息的 `subagent` / `terminal` 字段告知用户。这条提示属于**新会话提示**（`emptySession()` 的条件段落，与 `systemPromptOverridden` 同机制）而非 transcript 事件：它描述会话怎么装配，不是会话里发生了什么；当成事件发会把 placeholder 顶掉（`applyEvent` 末尾无条件 remove）并凭空造出一个「运行中」的 work block。
- **不做 spawn CLI 的兼容垫片**：让扩展版 `subagent` 在这里跑起来只有两条路，都否掉。(a) 干预它的进程自省（改 `process.argv[1]`、往 `process.env` 塞 `ELECTRON_RUN_AS_NODE`）——进程级全局 mutation，会被所有子进程继承（包括用户每一次 `bash` 调用），且完全建立在猜测扩展实现之上，直接撞「不看它怎么实现」的红线。(b) 插件自己 spawn `pi`——依赖用户装了 CLI，撞「不要求安装 pi CLI」的定位，还带来版本偏移、认证/代理各走一遍、子会话黑盒。扩展版依赖的 spawn 机制只是它在 CLI 宿主里唯一能用的手段，是缺陷不是特性；它的 `agents/*.md` 角色数据同样不吸收，理由见上面「不引入子代理角色文件」。
- **不做通用的「依赖宿主的扩展」拦截**：判据不存在。静态扫描源码找 `process.argv` / `spawn` 是认实现且误报漏报都高；认扩展名/路径直接撞红线；让扩展自己声明宿主需求需要新 API（又是插件私有扩展 API）。`subagent` 与 `vscode_terminal` 是仅有的例外，且理由与宿主的失败模式无关：它们是插件自有工具的名字，同名扩展工具必须屏蔽（见上），不涉及对扩展实现的任何判断。其他依赖宿主的扩展工具只能靠 README 的宿主边界说明让用户自己对号。
- **VS Code 能力的使用边界（红线）**：VS Code API 用在**呈现层**（面板、状态行、QuickPick、diff 视图）和**宿主职责层**（认证对话框、代理配置、插件自有能力的配置项），不用在 **agent 能力面**。判据是工具的**语义能否在其他宿主中重现**，而不是实现时碰没碰 VS Code API：「读取编辑器选区」「当前断点停在哪」在 CLI 宿主里**结构上不存在对应概念**，会让会话历史变成只在这一个宿主里有意义的东西，禁止；「查找符号引用」这类语义任何宿主都能实现（CLI spawn language server 即可），输出是 `文件:行:列` 纯数据，**不因实现手段而违规**。但**语义可重现只是必要条件，不是通行证**：它还得过下面「工具集」那条的结构性优势门槛——而**能用 pi 扩展做的能力就不具备结构性优势**（扩展跑在 Node 里，能 spawn 任何进程，且做完 CLI 与 GUI 共享同一份）。`subagent` 工具过关是因为它架在 **SDK 的 in-process 子会话**上，不是 VS Code API，CLI 宿主只能 spawn 独立进程 `--no-session`，结构上给不了。`vscode_terminal` 过关是因为它要的是三件事的**合取**（用户能直接敲 + agent 拿到完整 transcript + 聊天界面不消失）：它的语义在其他宿主可重现（那里就是终端），但 CLI 宿主要同时满足三件只能自己写终端模拟器塞进 TUI（SDK 自带的 interactive-shell 例子只能二选一）。LSP / symbols / diagnostics 工具属于前一类——语义可重现但无结构性优势（扩展 spawn language server 即可，且 CLI 与 GUI 共享），**已评估并否决**；它唯一的真实差异是 VS Code 的 language server 已热索引，而那是性能差异不是能力差异，且扩展未激活时的空结果与「确实无引用」无法区分（危险的假阴性）。需要新能力时，扩展由用户经 pi 自己的包管理器安装（`pi install` / settings 的 `packages`，见 `docs/packages.md`）；插件**不代管、不捆绑、不安装**任何扩展。插件对扩展只做**渐进增强**，且增强只能架在 **SDK 原生、宿主无关的接口**上（`ctx.ui.setStatus` / `setWidget` / `notify` / 斜杠命令）：它们描述内容而非布局，每个宿主都欠它们一个渲染。**不得为某个具体扩展定制 UI**（不认它的注册表文件、不认它的命令名），也**不得自创插件私有的扩展 API**（那只是把耦合反转到扩展侧，且 CLI 无对等物）；真缺能力时推动上游给 `ExtensionUIContext` 加。**上游若为某类宿主能力给出了标准接口（例如「向宿主查询语言服务」），插件实现它就不再是私有能力，性质同 `setStatus`，红线自然放行。**
- **提示词（红线）**：插件不向 system prompt 注入任何**行为引导**。工具 `description` 只陈述机制事实（并行、隔离上下文、scope 会被强制、不可嵌套、结果如何返回），不写 `promptGuidelines`、不写「应该/不应该何时用」。防滥用、工具优先级、并行切分策略这类策略属于用户的 `~/.pi/agent/APPEND_SYSTEM.md` 或项目 `AGENTS.md`，CLI 和 GUI 自然共享。**两件事不算注入**：① schema 字段与上限（机制，且比 prompt 可靠——**约束应当落在 schema 上而不是 prompt 上**）；② 告知子代理本次运行的参数（scope、cwd 这类，作者是主代理）。
- **自定义供应商只有「编辑 models.json」一条路**：自定义接入点/模型/API key 属于共享配置（`~/.pi/agent/models.json`，见 `docs/models.md`），CLI 与 SDK 都不提供编辑 UI。插件在供应商列表首行给一个入口打开该文件：文件为空时写入带注释的整份模板；文件已有内容但尚无供应商时向 `providers` 顶部**插入一条 provider 模板**（存为未保存的编辑以便 Ctrl+Z 撤销，也避免未填写的占位供应商直接进模型列表）；已配置供应商时直接打开、不再插入模板——模板只为从零开始的人存在，已有配置者打开文件就是继续编辑（pi 用 `stripJsonComments` 解析，注释是格式的一部分；文档语言设为 `jsonc` 以免 VS Code 报错），保存时经 `ChatBridge.reloadModelsConfig()` 做 `modelRuntime.refresh({ allowNetwork: false })` 并把 `getError()` 显示成 transcript 卡片（CLI 在启动/`/reload` 时也打印它，缺了它写错配置会静默失效）。**不做表单/向导**：models.json 的 schema 面太大（四种 api、十几个 compat 开关、cost 分层、thinkingLevelMap、modelOverrides），向导只能覆盖一小部分且用户最终仍要回到文件，同时它会变成 GUI 私有的配置编辑器，与 CLI 无对等物。允许的结构化写入**只有整条 provider 粒度的两个**：插入一条带注释的模板（文本插入，因为 `modify()` 写值会丢掉注释）与删除整个条目（供应商行的 🗑 按钮，`jsonc-parser` 的 `modify()`）；两者都用 `WorkspaceEdit`（保留注释与格式、与已打开的编辑器同步、可撤销），删除另经 `document.save()` 走与手改相同的重新加载路径；**不要再往里加字段级的写入**。注意 `jsonc-parser` 没有 exports map，esbuild 默认会选到它的 UMD 构建（内部 `require("./impl/format")` 无法静态解析，bundle 加载即报 Cannot find module），因此 `esbuild.mjs` 用 alias 钉到 `lib/esm/main.js`。
- 模型/思考等级的「会话级 vs 全局默认」分离：SDK 0.84.3 起 `AgentSession.setModel()` / `setThinkingLevel()` 默认只作用于当前会话，持久化全局默认需显式传 `options.persist`——这正是插件一直要的语义，旧版（≤0.84.2）在 `PiRuntime.setModel()` 里的「快照旧默认值再写回」对冲已删除。全局默认的两条写入路径不变：模型选择器行内的 📌 按钮（`setDefaultModel()` 直写 settings，不用 `persist: true`——它会连带切换当前会话的模型，不是纯 pin）与设置菜单的默认思考等级。改动这一段前先确认该语义。
- 模型与思考等级的**快捷菜单**画在 webview 里（`src/webview/picker.ts`，小弹层对齐 composer chip）：QuickPick 固定出现在窗口顶部，离被点击的控件太远。菜单只做切换：模型行只有名称 + 供应商，列表内容仅限常用模型（scoped），未配置时显示「无」而不是列出全部；搜索、能力详情、⭐常用、📌默认均由末尾「其他模型…」打开的原生 QuickPick 提供，`/scoped-models` 同理。宿主只负责数据与落盘（`models` / `openPicker` 宿主消息，`listModels` / `setModel` / `pickModel` / `setThinkingLevel` webview 消息）。
- **失败请求的手动重发**（`src/agent/resume.ts`）：一次请求没回来之后，继续任务的唯一办法曾是打一句「继续」——那条用户消息既进 transcript 也进模型上下文，却与任务无关。改为在一条带重试动作的通知上收尾：丢掉那条失败响应（**只丢 agent state，会话文件照 SDK 自己的做法保留**）再以空消息批走会话自己的 prompt 路径继续，等价于 SDK 自动重试在两次尝试之间做的两步，**不新增任何消息**（与互通红线无冲突：写入的仍然只是 SDK 会写的东西）。六条边界：
  - **必须走 `_runAgentPrompt` 而不是公开的 `session.agent.continue()`**：外层那圈循环才负责把会话标为 streaming、给这次尝试套上自动重试与自动压缩、并发出 `agent_settled`；直接调 agent 能把请求发出去，但 UI 会永远等一个不会来的 settle。
  - **私有入口必须特性探测**：探测不到就不提供这个动作（`supportsResume()`），升级后退化成「没有按钮」而不是「按钮失灵」；`diagnostics.ts` 的 `manual retry` 自检项钉住它。
  - **能不能重发只由宿主按会话状态判定**，不由按钮判定（通知会留在 transcript 里，点击天然可能过期），且只对「停在 `stopReason === "error"` 的响应上」成立：重发一轮正常结束的对话会把用户正在读的答案悄悄丢掉（`aborted` 同理：那是用户自己按的停止）。
  - **提供重试的时机是 `agent_settled`，不是 `auto_retry_end`**（`offerRetry()`）：只有那里自动重试/压缩/排队全部落定，「这一轮停在失败响应上」才是稳定事实，一条规则同时盖住三种失败（自动重试放弃 / 自动重试被关掉 / 压根不可重试的错误）；挂在 `auto_retry_end` 上只能盖住第一种，而且一次失败会长出好几个按钮。实时链路还必须保留 `message_end` 上 `stopReason === "error"` 这份**类型化事实**（`liveFailedResponses`），后续非错误响应清掉：用户已经看见 `Request timed out.` 时，不能仅因 `agent_settled` 当下 `SessionManager` 暂未把同一响应暴露为 branch tail 就漏掉按钮；这不是按错误文案猜测，回放仍以持久化 branch 为准。子会话不给：一路子代理挂了是汇报给主代理的事，用户对子代理只有「看」和「停」。
  - **回放时必须重新给出这个提议**（`withRetryOffer()`）：那条通知是 transcript **事件**，只存在于看见失败发生的那个窗口；重启后从会话文件回放只剩一条赤裸的 provider 错误（`Request timed out.`），无处可点——而使重试有意义的**状态本身是存活的**（失败响应仍是最后一条消息）。因此在 `postHistory()` 重算一次，且**只加在发给 webview 的副本上**，不写进内存历史（否则每次 attach 都会长出一条）；`diagnostics.ts` 的 `replayed retry offer` 自检项从会话文件一直钉到真实 post 出的 `history` 消息。
  - **带动作的通知不进 work block**（`ChatEvent` 的 `status.retry`）：work block 默认折叠，藏在折叠里的按钮不算「提供」。
- **扩展输出走 transcript**：pi 扩展的 `ctx.ui.notify` 不弹 VS Code 通知框（单行、会截断、脱离对话），而是经 `PiRuntime.setExtensionNoticeSink()` → `ChatBridge.emit()` 变成 `status` / `error` 卡片留在聊天区，多行自动折叠。扩展 handler 抛出的错误同理：`bindExtensions({ onError })`（SDK 的 interactive/rpc/print 三个 mode 都传，属应用入口职责）经 `setExtensionErrorSink()` 变成 transcript 里的 `error` 卡片，不再静默丢弃。两个 sink 都必须在首次 `bindExtensions()` 之前注入（`ChatBridge` 构造函数里），且闭包捕获被绑定的那个 session，子会话/preview 的隔离由 `emit()` 的 `histories` 自动生效。扩展命令执行期间（`extensionCommandDepth > 0`）标 `scope: "command"` 置于顶层并展开，其余（后台事件里的 notify）不标 scope、收进 work block 防刷屏。未接线 sink 时 notify 仍回退原生弹窗，不静默丢消息。
- **扩展命令上下文由宿主提供**：`ctx.newSession()` / `ctx.fork()` / `ctx.switchSession()` / `ctx.navigateTree()` / `ctx.reload()` 走 `bindExtensions({ commandContextActions })`，SDK 没有默认实现（缺省是“报告成功但什么都不做”的 stub，所以不接线就是静默失效），三个 mode 各自接线（最接近的是 `modes/rpc/rpc-mode.ts`），插件同理（`PiRuntime.commandContextActions()`）。会话替换有两条来源：**宿主发起**的（webview 动作 → `PiRuntime.newSession()` 等，调用方随后自己 `attach()`）与**扩展发起**的（在 SDK 内部完成，宿主只能经 `AgentSessionRuntime.setRebindSession()` 得知）；后者由 `SessionLifecycleSink.reattach()` 重建视图，前者靠 `replacingSession` 标志跳过，否则会 attach 两遍并重发一次 `session_start`。`navigateTree` 不换 session 对象、不触发 rebind 钩子，所以在 action 里显式 reattach。这组 actions **只给窗口正在显示的那个会话**（`bindSessionExtensions(..., { ownsSession: true })`）：子会话绝不能把父会话换掉。`ctx.reload()` 与 `/reload` 必须同一条路径（`SessionLifecycleSink.reload()`），否则两者的 UI 收尾会不一致。`diagnostics.ts` 的 `extension command context` 自检项钉住它：真实调一次 `ctx.newSession()`，会话要真的换掉且 reattach 恰好触发一次。
- **`setStatus` / `setWidget` 不是 TUI-only**：它们和 `notify` 一样描述内容而非布局，CLI 分别渲染在 footer 与编辑器上下方，所以插件也必须渲染（`setExtensionStatusSink` / `setExtensionWidgetSink` → `webview/statusline.ts` 的独立状态行 / `webview/widgets.ts` 的 composer 上下文本块）。它们曾落在 `createVsCodeExtensionUiContext` 的 no-op Proxy 里被静默吞掉——那是错误归类，会让用了这两个 API 的第三方扩展在 GUI 中无声失效。`setWidget` 的 component-factory 重载才是 TUI-only（基于 pi-tui 的 `Component`），检测到函数入参直接丢弃，不崩溃。这两类是**实时 UI 状态而非 transcript 历史**，因此不进 `histories`，按 sessionId 隔离、`attach()` 时清空由扩展重新发布（对齐 CLI 重新 bindExtensions 的行为），preview 下发空集。
- **工具 `details` 透传而非移植 `renderCall`/`renderResult`**：这两个钩子只产出 pi-tui `Component`，而 `Component` 唯一的出口是 `render(width): string[]`——已排版的 ANSI 行，**里面取不出内容**（作者意图在函数体的控制流里，是代码不是数据）；把 ANSI 转回 DOM 属于「TUI 呈现层特性」，不引入。真正结构化的是它们的**输入**：`args` 与 `AgentToolResult`。因此插件把 `details` 经 `agent/tool-details.ts` 清洗后透传，在 `webview/transcript.ts` 的 `renderDetailsBlock()` 画成**默认折叠**的通用树。三条边界：只给没有专用卡片的工具（`TOOLS_WITH_DEDICATED_CARDS` = pi 自带七个；`subagent` 虽有专用卡片但**不在列中**，因为它的卡片正是用 `details` 画的）；**不认任何扩展的 schema**（认了就是为具体扩展定制 UI，红线）；宿主必须清洗（details 是任意对象，含函数/循环引用会让 `postMessage` 的结构化克隆直接失败，超大 payload 会刷屏）。
- **webview 的「当前显示什么」是单一联合类型（红线）**：`bridge.ts` 的 `View` = `live`（主会话、可输入）/ `lane`（活的子会话，实时进展、可单独停）/ `replay`（只用于从会话文件重放已经没有 live child 的历史子代理，`laneTitle` 必填，仍按子代理呈现）。`displayedSession` / `inputDisabled` / `delegation` / `transcriptId` **全部从它派生**，切视图只经 `setView()`。这一条是代价换来的：它们曾是三个独立字段（`displayedSession`/`displayedLaneId`/`preview`），合法组合靠人每次记得同步，同一个 bug 修了三轮都只改对了链路的一段。**新增派生状态时不得新加平行字段**，否则等于把该类错误重新引入。
- **宿主侧状态必须由宿主侧测试守**：`scripts/smoke_webview.mjs` 手工构造 `ChatState`，绕过了 `postState()`，因此**对宿主侧 bug 完全不可见**——测试与实现共享同一盲区时，绿色是假的。`diagnostics.ts` 的 `view state` 自检项驱动真实 `ChatBridge.handleMessage()` 并读 `postState()` 实际 post 的内容，改视图逻辑后必须跟着扩充。
- **webview 的阅读位置按 transcript 记忆**（`transcript.ts` 的 `TranscriptViewState`）：展开的 work block、消息区滚动、`.work-body` / `.tool-body` 内部滚动。`applyHistory()` 每次全量重建 DOM，而子代理鼓励用户频繁进出，不记则每看一眼就丢一次位置。key 用**位置序号**（work block）与 **toolCallId**（卡片）；身份由 `history` 消息的 `transcriptId` 给出，两层 Map + LRU 上限 8（往返必然经过另一个 transcript，单层会必丢）。
- **正式消息只有「每个角色最新一条」默认展开**（`webview/bubble.ts`）：更早的**长**消息折叠成预览（CSS 裁剪 + 渐隐，不重建 DOM），短消息永不折叠——为省两行而多一次点击是纯负担。四条边界：① **是否够长按 Markdown 源文本判定**（行数阈值来自插件自己的设置 `piAgentChat.transcript.foldLines`，默认 14；无换行的长文本按每约 50 字符折算成一行计入同一预算，`0` 为永不折叠；经 `foldThreshold` 宿主消息推送，变更后由宿主重放 transcript 让已建气泡重新判定），不按渲染高度——无头快照里每个元素都测得 0，用测量就会与屏幕上不一致，回归也就测不出来；② **用户手动开合过的气泡（`pinned`）不再被自动折叠**，该决定按位置序号记进 `TranscriptViewState.bubbles`，与 work block 同一套记忆机制，切走再回来仍然作数；③ 渲染出的 Markdown 必须待在 `.bubble-content` 里，裁剪只能作用于它——徽章、页脚与 hover 动作条都在 `.bubble` 上，被一起裁掉就再也点不到了；④ **不跟随最新输出时（`followBottom === false`）自动折叠一律延后**（`transcript.ts` 的 `deferredFolds`）：用户滚上去就是在读旧内容，而规则要折的那条往往正是他在读的那条，折下去文字当场消失、下方全部跳位。此时只记账不执行，等跟随恢复（跳到底部按钮 / End / 发送 / 自己滚回底部，`resumeFollowing()` 与 scroll 处理里的 false→true 迁移）再一次性补折，补折后若仍在跟随则重新贴底。**回放豁免**（`replaying` 为真时立即折叠）：回放重建的是整份 transcript，结果不能取决于用户当时站在哪里——否则无头快照与真实窗口会得出不同 DOM。
- **复制写剪贴板走宿主**（webview 的 `copyText` 消息 → `vscode.env.clipboard`，同 `openFile`）：webview 里的 `navigator.clipboard` 受焦点与权限限制，desktop / remote / 浏览器宿主表现不一致；按钮乐观回显✓，失败由宿主自己报。代码块的复制按钮在 `markdown.ts` 的白名单净化**之后**注入（marked 没有这个能力，也不为它引第三方依赖），因此包裹元素与按钮不在白名单里也能存活。
- **输入历史的方向键只在文本外层行被劫持**（`webview/composer.ts`）：↑/↓ 回溯/前进发送历史（webview 侧内存记录，全局共享、上限 100、连续重复不入栈，导航起始时暂存草稿并在回到最新时恢复；条目存**原文**与 `@` 引用 chips，两者一并回填，离开某个历史位置时把当前编辑写回该槽位，清空视为放弃不写回，发送即结束导航）。触发条件是「光标（无选区）在第一行/最后一行」而非文档绝对首尾：单行输入要对齐 readline 的肌肉记忆（每次按键走一条，否则回溯第二条起每次要按两次），而多行草稿中间的方向键必须留给光标移动；IME 组合中（`isComposing`）不触发，补全面板打开时补全优先。历史必须在 webview 侧记录：会话文件存的是展开后的正文，回放拿不到用户敲的原文。**会话 attach 时回放 populate**（对齐 CLI `renderInitialMessages` 的 `populateHistory`）：宿主只在「会话成为 live」的 history 消息上标 `populateInputHistory`（`attach()` 与 `ready`，其余重放一律不标），webview 遍历 `user_message` 入栈（存展开/折叠文，与 live 原文同一环，CLI 同构）并**按 transcriptId 去重**——窗口启动 attach+ready 双发、lane/preview 往返重放都不得重复灌入；lane 不灌是硬规则：子会话的首条「用户消息」是主代理写的任务文本，不是用户敲的。已知小缺口：`navigateTree` 不换 session 对象，同会话切分支后新分支独有的消息不会重新 populate（那些消息若在本窗口发过则已由 live 路径入栈）。
- **Webview 有窄 / 宽两种响应式布局**：`main.ts` 用同一个 `ResizeObserver` 切换（事件驱动，不轮询）。**阈值是独立配置项 `piAgentChat.layout.wideModeMinWidth`（默认 1200），不再由列宽推导**：旧实现把阈值算成「中栏配置宽度 + gutter + 两侧栏最小值 + chrome」，等于把「正文能多宽」和「侧栏何时出现」绑成一件事——调宽正文会莫名其妙地把侧栏推远。宿主侧 `readWideThreshold()` **必须把它夹到 ≥ `WIDE_THRESHOLD_MIN`**（= `CENTER_MIN_WIDTH` 480 + gutter 44 + 2×`RAIL_MIN_WIDTH` 230 + chrome 48 = **1032**）：低于此三栏无法同时满足各自最小宽度，放行等于让布局切进一个它满足不了的形状。**跨过阈值不打开任何东西**：它只是让侧栏「成为可能」，并把 header 的「会话列表 / 资源」两个开关从窄屏语义（整页 / 顶部浮层）切成宽屏语义（停靠栏）。因此阈值可以放心调低——不开栏就不花钱，也不会有一次「窗口一宽，界面自己重排了」的惊吓。
- **宽屏是可拖拽三栏，几何只有一个作者（红线）**：`grid-template-columns: var(--rail-sessions) var(--split-sessions) minmax(0,1fr) var(--split-resources) var(--rail-resources)`。**分隔线占真实 grid 轨道而不是坐在 `gap` 里**，这样关掉一栏能连它的分隔线一起收掉，中栏才真的接管那块空间（`gap` 恒为 0；两条 12px 分隔线 + 两侧 12px padding 就是阈值预算里的 48px chrome）。**侧栏贴 webview 两边，中栏在剩余空间里居中**（`max-width: var(--chat-column-width); margin-inline: auto`）——这条是「窄→宽连续」的全部依据：窄屏本来就是「正文居中、余量变对称边距」，侧栏贴边则跨阈值只是两条栏从边上滑进来，**正文一个像素都不动**；若改成「整块三栏居中」，跨阈值的一瞬间正文会横向跳一下。**所有几何决定都在 `webview/splitter.ts` 一个地方**，经四个 CSS 变量落到样式表，`_wide.scss` 只声明哪条轨道读哪个变量，自己不做尺寸决策。三条规则全是 clamp 而不是模式：① 侧栏不得超过 `RAIL_MAX_WIDTH`（420）——**理由与中栏的上限不同**，侧栏装的是单行标签，受**截断**约束而非行长约束，过了不再产生截断的宽度，多给的像素只是在每个标签右边堆空白；② 拖到低于 `RAIL_MIN_WIDTH`（**180**）**即关闭**该栏，「开在最小宽度」与「关闭」之间没有中间态，这才使拖拽成为一种关闭手段而不是把栏拖废的手段（**这个值取的是「侧栏干不了活了」而不是「看着拥挤」的点**——低于它会让会话行的元信息行换行；定高了就把「拖拽变成关闭」前面那段宽度区间整个拿走了），**它也是 `WIDE_THRESHOLD_MIN` 的因子**，改它必须同步 `package.json` 里 `wideModeMinWidth` 的 `minimum`（那是该派生值的第二份拷贝，只有设置界面读它，漂移了运行时不报错，只会允许用户选一个布局满足不了的阈值；`diagnostics.ts` 的 `manifest wide-threshold floor` 自检项钉住它）；**但它不是初始宽度**——没拖过的栏开在 `RAIL_DEFAULT_WIDTH`（280，双击分隔线也重置到它）：最小值标的是「再窄就干不了活」的那一点，把它当默认值等于让每条栏都从自己最差的形态开场；③ 中栏不得被挤到 `CENTER_MIN_WIDTH`（480）以下，拖到那里就停住、**绝不关闭任何东西**。拖拽关闭与点 header 开关是同一个决定的两种手势，必须写回同一处状态（`initSplitters(onClosed)` 回调）。**分隔线默认不画任何东西**：两条 rail 都已声明自己的边界（边框 + 差异底色），在 gutter 里再画线是给两个本来不相接的东西加第三条边；可拖拽性靠 hover 时的 `col-resize` 光标告知（那正是人在找这个 affordance 的时刻），12px 命中区始终存在，只有涂色是条件的。**`splitter.ts` 绝不测量 DOM 取宽度**：可用宽度由 `ResizeObserver` 经 `setAvailableWidth()` 喂进来。测量只有在「表面已按新尺寸布局完」之后才有意义，而这里每个调用点都不满足——宽窄切换刚重写完 grid、无头环境根本不布局；那一刻测出来是 0，于是每条栏都「显然摆不下」而被当作用户拖关（jsdom 冒烟实测踩过，两条栏在窄→宽往返后永久消失）。**几何常量在 `shared/protocol.ts`**（`RAIL_MIN_WIDTH` / `RAIL_DEFAULT_WIDTH` / `RAIL_MAX_WIDTH` / `CENTER_MIN_WIDTH` / `CHAT_COLUMN_GUTTER_WIDTH` / `WIDE_GRID_CHROME_WIDTH` / `WIDE_THRESHOLD_MIN`），与 `_wide.scss` 的轨道声明、`splitter.ts` 的 clamp、`_tokens.scss` 的 `--content-gutter` / `--chat-column-width` 互为镜像，改一处必改其余；改完用 `scratch` 里的真实 Chrome 扫一遍（见 `webview-browser-debug` 技能，注意样式表在 **`dist/main.css`**）——DOM 快照看不出这类回归。
- **不自动打开侧栏（红线）**：进入宽屏、以及任何 resize，都不得打开用户没打开过的栏（`resources-view.ts` 的 `panelState.wide.shown` 与 `main.ts` 的 `wideRailsOpen` 两处默认值均为 `false`）。但**「恢复」不是「自动打开」**：用户开过的栏与拖过的宽度都持久化在 webview state（`wideSessionsOpen` / `wideResourcesOpen` / `railWidths`），窄↔宽往返、窗口重开都要照原样回来——把一个刻意的选择在每次 resize 时丢掉是另一种 bug。窄模式保持原交互：会话列表是替换聊天的整页，资源是 header 按钮控制的顶部折叠面板。资源的**可见状态与展开状态按布局模式分成两份**：用户在某一模式里的开合只写该模式，来回切换恢复各自状态，不得继承另一模式；**展开默认值两模式仍不同**（宽屏栏是用户特意开的，折叠着就没东西可看，故**开即逐层全展开**——给了它一整列就是为了看见里面的行；窄屏浮层压在 transcript 上，故起手是一行摘要、逐层打开）。资源内部各 section 的**显式开合**仍两模式共享——那是内容偏好，不是布局偏好；因此记的是「用户的决定」（`sectionExpansion` 是 `Map<string, boolean>` 而不是一个已展开集合），没有决定才落到当前模式的默认值，两个默认值才不会把用户的选择吃掉。
- **中栏最大宽度 `--content-max-width`** 由 VS Code 配置 `piAgentChat.layout.contentMaxWidth` 控制（默认 950，**只有下限 500、没有上限**）。**它现在只表达字面意思**：消息区最多能长多宽。上限被删掉是因为唯一支撑上限的理由（这个值同时决定宽屏何时开启）已经随阈值独立而消失；5K 屏用户想要 2000px 正文不是一个需要被纠正的错误。webview 读不到设置，宿主在 `ready` 时经 `contentWidth` 消息把**列宽与阈值一起**推送（每次 ready 无条件重推——控制器交换会重赋 `webview.html`、webview 整个重载，新实例只认识文档默认值），配置变更时即时推送；webview 收到后写入 CSS 变量、存入 webview 持久化状态（`setState`，重载后存活，首次布局前恢复，避免宽窄判定依赖消息时序）。两个值同行推送是因为它们服务于同一个判定，缺一个就得等第二条消息。三种 surface 使用同一份 `--surface-bg`（映射 `sideBar-background`），`#root` 仍铺满 webview，不能对整个根节点设 `max-width`。header 继续横跨全宽：editor / 新窗口隐藏重复的会话标题，辅助侧栏保留标题，六个文字按钮贴 Webview 最右侧。窄模式新增内容用 `.content-column` 共享同一宽度上限；宽模式新增顶层区域必须明确属于左栏 / 中栏 / 右栏。composer 的层次阴影仍放在限宽 `.composer-content`，不得用全宽阴影在中栏两侧造暗条。**输入区相对消息列的几何是恒等式**：输入框比消息列每侧宽 10px（composer 内缩 12 对 `.messages` 内缩 22）。两列同时被上限截断时，消息列填满上限、composer 却要把内边距算进去，这个外伸会反转成每侧窄 12px——所以 `.composer-content` 的上限是 `--chat-column-width`（= `--content-max-width` + `--content-gutter`，44 = `.messages` 的左右内缩和：22 + 12 + 10px 稳定滚动条槽），使任何宽度区间都保持同一相对关系；改 `.messages` 内缩时必须同步 `--content-gutter` 与 `CHAT_COLUMN_GUTTER_WIDTH`。
- **会话列表跨 surface 同步走窗口级事件，不做轮询**：`ChatSurfaceManager` 持有一个窗口级 `EventEmitter`；任一 controller 的 `ChatState`、claim / slot 位置、删除或重命名发生变化时发出 invalidation，所有 `ChatBridge` 订阅后只在自己的会话页 / 宽屏左栏可见时合并刷新（300ms trailing debounce，避免一轮 SDK 事件重复扫描 JSONL）。sidebar 与 editor / 新窗口因此读到同一批状态变化；不可把这个机制改回 webview 各自定时扫盘。会话列表中只有 `.sessions-list` 是滚动容器，标题与搜索框固定；实时重建列表时保留已渲染批次数和 `scrollTop`，不能因状态事件把用户弹回顶部。
- **基样式 `button:hover` 必须用 `:where()` 保持与 `button` 同特异性**（`src/styles/_base.scss`）：写成直觉的 `button:hover:not(:disabled)` 会拿到 (0,2,1)，**盖过所有只用类选择器的 hover 规则**（(0,2,0)），于是卡片头、列表行、气泡里的小按钮这些静止时扭平的按钮，一 hover 就亮成主按钮的**蓝色方块**（而且它们自己的规则看起来完全正常，很难归因）。曾经用一份穷举类名的清单打过补丁，但那要求每个新增的扁平按钮都记得去登记。
- **颜色只有两层，语义色只有四个（红线）**：`_tokens.scss` 左边是「意思」、右边是 `var(--vscode-*, 回退)`，**其他规则只允许用左边那一列**；组件规则里出现字面颜色、或多出第三层别名，就是这套样式表停止跟随用户主题的方式（复查：除 `_tokens.scss` 外 grep `#[0-9a-f]{3,8}|rgba?\(`，命中项必须全在 `var(..., 回退)` 的回退位）。语义色**只有四个**：`--signal`（交互与用户自己的声音，`textLink-foreground`）、`--pulse`（运行中，`charts-blue`）、`--verdant`（生效过 / 成功，`charts-green`）、`--ember`（失败，`charts-red`）。三个状态色同出 `charts.*` 是故意的——那是一套**设计时就互相可区分**的颜色，而 `errorForeground` 在 Dark+ 是偏橙的三文鱼 #f48771，读得出「警告」读不出「错误」。**运行中不得用暖色**（UI 词汇里暖色是「警告」，而「跑得好好的 agent」是本产品最常见的状态），**也不得用 `button.background` / `progressBar.background` 这类 primary 变量**：它们是**为底色设计的**，Dark+ 下对 `#1e1e1e` 对比度只有 2.6 / 3.3，而 `--pulse` 有一半用法是 10.5px 小字（charts-blue 为 4.99）。**子代理不占语义色**（它是工具，不是状态）。**hover 反馈一律用灰**（`--hover-bg`），蓝色只给**静止时就该是蓝的**东西（主按钮、链接文字、列表选中态、焦点环）。**`*-soft` 淡底只有两个（`--signal-soft` / `--ember-soft`），不为对称补齐**：填充是很重的表达，「运行中」与「生效过」都不需要——前者一条蓝边就够（`.work-block.running` 曾同时上边框、淡底和扫描带，淡底让一个跑着的块比旁边那段回答还重，而那是本产品最常见的状态），后者靠文字颜色。补齐会得到四个名字里只有两个是真的。同理**用户气泡不上淡底**：它不是状态，不需要注意力，右对齐加一圈框已经说完「这是你写的」；它的底色是 `--bg-raised` 而不是透明，只因为 hover footer 靠 `background: inherit` 浮在右下角，需要一块不透明的面才不会压住正文。
- **transcript 是一次 agent 运行的日志，不是对称聊天（红线）**：两个声音靠**位置与重量**区分而不是一对镜像气泡。**两条消息都有框，靠「锚在哪一侧 + 哪条边是彩色的」区分，而不是靠一方有框一方没有**：agent 左对齐 + `--verdant` 左边，用户右对齐 + `--signal` 右边，其余三边都是同一条 `--border` 细线。曾经试过「agent 消息完全无框、用 byline 当上沿、用一条 16px 短线当下沿」，实机否决：一段回答读不成一个独立对象，而那条短线和 byline 加起来比一个边框还碎。**两侧都不上淡底**（见上面 `*-soft` 那条），所以框是唯一新增的东西。推论：**agent 侧的 footer 回到绝对定位**（`background: inherit` 现在有不透明底可继承，且 padding 保证它不压最后一行）；agent 气泡**用 margin 而不是 max-width 预留右侧留白**——它是 `align-self: stretch` 的 flex item，cross size 由 flex line 决定，实测 `max-width` 干不过 stretch。**折叠用 `mask-image` 而不是盖渐变遮罩**（遮罩要知道背景色，而 agent 消息已经没有背景色了），不透明端用 `currentColor`（mask 只读 alpha，写字面色只是多一个硬编码）。
- **「执行过程」三态的重量分配，唯一依据是「需不需要你看一眼」**：**折叠 = `--line-soft` 细边**、**展开 = `--border` + `--section-bg`**、**运行中 = `--pulse` 边 + `workScan` 扫描带 + 脉冲点**（多重冗余是故意的，光靠颜色会同时失败两次；**冗余不含淡底**，见上面 `*-soft` 那条）。折叠态最初是**完全无框**，理由是「给每个已完成轮次都套盒子，框就不再表示这里有东西可以打开」——理由成立但**用过头了**：agent 自己的消息也没有框，于是一个跑完的执行过程根本不再读成一个独立对象。`--line-soft` 同时回答两边：够把它划成一个对象，又弱到一整列不会变成一摹盒子。同理 `.work-label` 用 `--text-dim`（一个已完成的块是旁边那段回答的背景，不该是屏幕上最亮的一行字），**运行中也一样**：标签曾在 `.running` 下提到 `--pulse`，已删——它与旁边的摘要是同一句话的两个字段，两种颜色会读成两个不相干的东西，而边框 + 扫描带已经把状态说完了。CSS 里用 `:not(.collapsed)` 而不是 `.open`——`collapsible.ts` 只 toggle `.collapsed`。**标题栏四个文字字段的顺序是机制而不是排版**：`.work-label`（执行过程）・`.work-status`（思考 N 段 · 工具 M 次，`flex: 0 0 auto`，永不截断）・`.work-failures`（失败 N）・`.work-action`（正在调用 X / 最近 X，`flex-shrink: 100` + 省略号）。三个尾字段各自独立而不能拼进 `.work-status`：被截掉的必须是「此刻在干什么」（它长度无界且尾巴最不重要），而不是计数或失败数（380px 实测踩过）。**工具行与思考行同样有整框**（`--border` 细边，左边固定 2px 留给状态色，运行中/失败只换颜色不换宽度，body 靠顶部 hairline 归属）：它们曾经是无框平行，理由是「已经在执行过程的框里了，再包一层就是盒子套盒子」——但同一个 body 里还会出现 `.notice-card`（重试、压缩提示），它们天生带框，于是平行的那几条在旁边读成了另一种没做完的东西。统一给框是更便宜的一致性。
- **分隔用留白与色调，不用横线，也不用投影（红线）**。两处横线已删：轮次分隔改成 `.bubble.user.turn-open` 的 14px 上边距（= 双倍的 12px flex gap；再大会在列里留下空洞），压缩边界改成与折叠块同形的细框。理由：满列宽的线是眼睛每次滚动都要跨过的家具，而用户气泡已经靠底色与右对齐把边界说完了。**投影全部删除**（原本五处：header 向下、messages 内阴影、composer 向上、两个弹层）：一套扁平界面下的深色光晕读成的是脏污而不是层次，而每个弹层本来就带边框 + 不透明底色，阴影是纯装饰。需要声明区域边界时用 hairline（header 的 `border-bottom`），或者**给那个块一个自己的面**——扩展 widget 区（`ctx.ui.setWidget`，如 services）与 transcript 之间原先只靠 composer 的向上投影分界，投影删掉后改为给 `.widget` 自己上 `--bg-raised` + 一圈 `--border`：边界成了那个块的属性而不是横跨整列的一条线，且**空的时候天然没有边界**（没有 widget 就没有盒子，不会在输入框上方留下一条无主的分隔线）。
- **侧栏是抬起的面，不是凹下去的槽**：宽屏两条 rail 与窄屏资源浮层统一用 `--bg-raised`（由 `--surface-bg` 向 `--text` 混出）。**不能用比画布更深的底色**：停靠栏是搭在聊天区旁边的面板，比周围暗会读成窗口上被挖了一个洞。**也不能钉到某个 `--vscode-*` 表面变量**：平台没有任何一个变量的语义是「比你当前所在的面高一档」，而这个 webview 在三种 surface 上都保持 sidebar 色调，所以抬起只能从它派生。向 `--text` 混也同时管对了两种极性（暗色主题变亮、亮色主题变暗），与 VS Code 自己在 editor 和 sidebar 之间做的是同一个动作。同一个底色让资源面板在窄↔宽切换时仍是同一个可识别的对象。`--bg-sunken`（不透明 editor 底色）只留给**真正嵌入的面**：工具输出块与代码块。
- **滚动条走 `::-webkit-scrollbar-*`，但必须先把标准属性交还给 `auto`（红线）**：webview 宿主会向每个 webview 注入 `@layer vscode-default { html { scrollbar-color: <slider> <editor-bg> } }`（`resources/app/out/vs/workbench/contrib/webview/browser/pre/index.html`），而 Chromium **按元素**选渲染路径：只有当该元素的 `scrollbar-color` 与 `scrollbar-width` 计算值**都是 `auto`** 时，文档里的 `::-webkit-scrollbar-*` 才会生效。宿主那条会继承到每个元素，所以**默认状态下整份文档都在标准路径上，webkit 规则全是死代码**——能解析、能进样式表、DevTools 里看着也正常，然后什么都不做，本视图因此顶着 Windows 原生滚动条（灰槽 + 上下步进箭头）撑过了两轮「修复」。**但标准路径给不了想要的结果**：这个平台上 `scrollbar-width: thin` **照样画上下步进箭头**（Chromium 的 Fluent 滚动条），实测判据是「点击滚动条端点，内容只移动一行（40px）而不是一页」——旧文的「`thin` 才是去掉箭头的开关」是错的，标准属性里根本没有关箭头的开关。因此现在的做法是用未分层的 `* { scrollbar-color: auto; scrollbar-width: auto }` 把控制权夺回来（宿主那条在 cascade layer 里，插件的规则未分层，**同属性下未分层必胜**，不需要 `!important`；`scrollbar-color` 继承而 `scrollbar-width` 不继承，所以重置必须落到 `*` 而不是 `:root`），再用伪元素表达全部外观：`::-webkit-scrollbar { width: 10px }`、**`::-webkit-scrollbar-button { display: none }`（这才是关箭头的开关）**、track / corner 透明（没有槽）、thumb 默认透明。**揭示由滚动驱动而非 hover**：滚动条回答「我在这东西的哪里」，只在内容正在动时才是问题；hover 版两个方向都错（停在 transcript 上读字会亮起一条什么都没报的杠；从别处滚轮滚过来又什么都不显示）。CSS 没有「正在滚动」状态，故由 `src/webview/scrollbars.ts` 在 **document 上挂 capture 阶段的 `scroll` 监听**（`scroll` 不冒泡但会捕获）给被滚元素加 `pi-scrolling`、静置 900ms 摘掉，`.pi-scrolling::-webkit-scrollbar-thumb` 才上色；伪元素规则**一律不加选择器限定**，天然覆盖所有滚动容器（含后建的代码块与弹层），**不需要也不要再维护滚动容器选择器清单**（曾有过一份 `$scrollers`，已漂移到让所有代码块的横向滚动条永久透明）。`pi-scrolling` 属于**瞬时装饰而非结构**，`scripts/smoke_webview.mjs` 的 `TRANSIENT_CLASSES` 必须把它从 DOM 快照里滤掉——它在不在取决于跑到那一步花了多久，记进基线就会快机器过、慢机器挂。**验证必须在带宿主注入层的页面里做**：`scratch/repro.html` 已镜像那段 `@layer vscode-default`，**不许删**——没有它，repro 与产品的滚动条行为根本不是一回事，这正是前两轮误判的唯一原因。**也不能靠看截图**（细半透明条在深色底上几乎不可见）：读 `getComputedStyle(el, "::-webkit-scrollbar-thumb").backgroundColor`、`::-webkit-scrollbar-button` 的 `display` 与 `offsetWidth - clientWidth`，箭头有无则用上面那条点击位移判据；若要数像素，**必须先把容器滚到中间**（transcript 默认停在底部，向下滚不产生任何 `scroll` 事件）**并注意截图耗时可能超过 900ms**（class 早在成像前就被摘了），两者都会把好机制测成 0。
- **transcript 里不出现时间（红线）**：SDK 只在 `SessionEntry` 上盖时间戳，而实时的 `user_message` / `assistant_message` 事件在 entry 落盘**之前**就发出了，那一刻根本没有 SDK 时间可用。因此：① **不得在 webview 或宿主读自己的钟**充当消息时间（那是在给「浏览器何时绘制」计时）；② **不得只在回放时画时间**（同一段对话重载前后长得不一样，而用户感知不到原因）；③ 轮次边界因此只是一段留白，它的职能（标出新指令的起点）本来就不需要时间。逐次工具耗时同理不做（SDK 不提供）。
- **流式光标属于某一条消息，不属于会话**：`.bubble.streaming` 由 `createStreamingBubble()` 挂上、由 `clearStreamingCaret()` 摘掉，而 `ensureWorkBlock()`（agent 从说话转到干活）与「不再 streaming」都要摘。写成「会话在跑就给最后一条消息挂上」是错的：那条消息早已写完，光标却在声称它还在长。光标是**伪元素**（`::after` 挂在正文最后一个块级子元素上）而不是插入的节点：增量到达时整段 Markdown 每帧重渲染，真节点会被每秒销毁重建约 20 次；伪元素还能在 class 摘掉的瞬间自己消失。对齐用小负值 `vertical-align` 而不是 `text-bottom`——后者把光标钉在降部线上，会明显低于它尾随的小写字母。agent 消息前**不再有 `Pi` byline**（框和绿色左边已经说明是谁在说话，再加一行标签是第三遍），随之删掉的还有 `.who` / `.live` 与 `agentName` / `agentStreaming` 两个词条。
- **会话行的 2px 条只表「运行中」**：「当前」与「运行中」是两件正交的事，共用同一形状同一颜色就是歧义；当前会话靠 `--signal-soft` 选中底色已经说完。同理「已在另一处打开」徐章用**中性灰**——它是本窗口 claim 的**信息**，不是会话的状态。**徐章放在元信息行行首而不是标题前**：放标题前会把标题顶右，于是只有部分会话在跑时整列标题左缘参差，而标题正是眼睛往下扫的那一列。行动作用**绝对定位 + 文字 mask 淡出**（参与布局会让 hover 时标题跳位），不可用的动作**置灰而不抽走**。
- **composer 是一个框**（`.composer-box`）：chips + textarea + actions 属于同一件事，分三条各自带框会读成三个不相干的控件。框、底色与**焦点环都在盒子上**（`:focus-within`），textarea 自身必须是裸的，否则是框套框。
- **代码块高亮用 highlight.js（而不是 shiki），颜色映射到 VS Code 主题变量**（`src/webview/highlight.ts`）：shiki 复现的是 VS Code 自己的 TextMate 语法，但要带上语法包 + WASM 正则引擎（体积以 MB 计、异步加载），**而且它同样拿不到用户主题的 token 颜色**（webview 只能拿到 workbench 颜色，拿不到 `editor.tokenColorCustomizations` 背后那套），差异不值这个体积。三条边界：① 用 `highlight.js/lib/core` + **手选语言子集**（全量 190+ 语法包对侧边栏是浪费）；② **只高亮 fence 声明的语言**，未标注/未注册一律保持纯文本——auto-detect 在聊天里常见的短片段上猜得很差，**错的颜色会被读成错的代码**；③ 颜色只用 `--vscode-*` 变量（symbolIcon / debugTokenExpression / charts 系列，逐个带 fallback），**不引 highlight.js 自带的主题 CSS**（硬编码颜色，不跟随明/暗/高对比度主题）。流式回答每帧都会重渲染整段 Markdown，所以高亮结果带缓存，超大代码块（`MAX_HIGHLIGHT_CHARS`）直接不高亮。
- 每次 `runtime.session` 被替换（new/resume/fork/tree）后必须重新 `bindExtensions()` 并重订阅事件 —— 走 `ChatBridge.attach()`。
- **启动时打开哪个 session：sidebar 记住上次归属的那个，每个编辑区 tab 记住自己的那个，而不是磁盘上最新的那个**。两者的记忆载体不同且不可混用：sidebar 用 `context.workspaceState`（`ChatSurfaceManager.startupSession()`，兼容读取旧的 `piAgentChat.lastSession`）；**tab 用 webview state**（webview 侧 `setPersisted("session", …)` → serializer 的第二个参数 → `restoredSessionFile()`），因为 VS Code 对每个保留下来的 panel 各调一次 `deserializeWebviewPanel` 并只交还它自己的 state，N 个 tab 需要 N 份记忆，共用一个 workspace 键必然互相覆盖（`piAgentChat.lastSession.editor` 已因此退休）。webview 只在 live 主会话上写这份记忆——lane / replay 显示的是别人的 transcript。`SessionManager.continueRecent()` 表达不了「用户切回旧会话」和「用户停在尚未落盘的空会话」，所以 `BridgeHost.rememberSession(file)` 仍由 `ChatBridge` 在 attach / 主会话事件时同步，`undefined` 表示空会话。`PiRuntime.create({ startup })` 的 `new` / `recent` / `file` 三态不变；**tab 恢复时缺文件或已被 claim 一律降级为空会话**（不是 recent——那会打开一个与这个 tab 无关的会话）。`diagnostics.ts` 的 `startup session` 与 `chat surface coordination` 自检共同钉住这些边界。
- 不要与终端 Pi 同时 resume 同一个 session（JSONL 追加写无锁）。
- 代理与 HTTP dispatcher 与 CLI 保持行为一致：`src/agent/http.ts` 复刻 SDK 未导出的 `core/http-dispatcher.ts`（`applyHttpProxySettings` + `configureHttpDispatcher`）。代理优先级 **环境变量 > `~/.pi/agent/settings.json` 的 `httpProxy` > VS Code `http.proxy`**；前两级就是 CLI 的顺序，VS Code 一级只能填 CLI 会直连的空位，因此插件不会与 CLI 冲突。`httpIdleTimeoutMs` 改变后必须重建 dispatcher（设置菜单的 `apply` 钩子）。

### 「CLI 有、SDK 没导出」的能力如何取舍

本项目只消费 SDK 的**公开导出面**（exports map 仅 `.` / `./rpc-entry` / `./client`），在 GUI 层做体验优化。遇到「Pi CLI 有、但 SDK 没导出」的能力，按以下四类判定：

1. **TUI 呈现层特性** → 不引入。如 mermaid 渲染、主题选择器、`registerMarkdownTransformer`、`tuiMode`。GUI 有自己的呈现方式，要做也是独立设计，不是移植。
2. **SDK 内部纯逻辑 helper** → 不复刻。要么用平台/自己的等价方案，要么放弃该功能（例：`normalizeWindowsShellPath` 曾被复刻为 `agent/paths.ts`，后按本规则删除）。
3. **应用入口职责**（CLI 在 `main.ts` 层做、任何宿主都绕不开的事）→ 必须实现。插件是 `main.ts` 的**同位体**，不是它的消费者。验收标准是「与 CLI 行为一致」，且代码里必须注明对应的 SDK 源文件路径，便于升级时比对（例：`agent/http.ts`）。
4. **共享配置的语义**（`~/.pi/agent/` 里的设置项）→ 必须与 CLI 一致。同一份 `settings.json` 两边行为不同，比没有这个功能更糟。

配套约束：

- **禁止 deep import 进 `dist/`**（绕过 exports map，升级必炸）。
- 落入第 3/4 类时，长期正确动作是**推动上游导出**，而不是永久维护镜像。
- 镜像 SDK 内部实现的位置统一用 `SDK-MIRROR:` 标记注释（并写明对应的 SDK 源文件）；SDK 升级后用 `rg -n "SDK-MIRROR" src` 逐一比对。
- **资源面板的配色语法只有三态**，新增行不得引入第四种含义：常规前景色 = 已加载且生效；绿色 = **本会话中生效过**（技能加载、工具调用、提示词调用、扩展的命令/工具/事件钩子跑过、上下文文件随请求发给了模型）；灰 + 斜体（`inactive`）= 已配置但未生效。这里是**宽口径**：绿色回答「在这次对话里真的起作用了吗」，不是「用户看见它发生了吗」，所以多数扩展在 attach 后即绿、Context 在首轮请求后整栏转绿，属预期。绿色有两个来源并取并集——transcript 能看见的（`webview/resources-view.ts` 的 `mark*` 系列）与只有宿主能看见的（`ResourceItem.used`，见 `agent/activity.ts`）。不要再把“次要信息”用灰色表达，否则“未生效”就读不出来了。GUI 不列 `Themes`（插件只用 VS Code 主题变量，pi 主题在这里不生效）。**面板只列 pi 官方的资源类型**：单个扩展发明的目录约定（例如 `~/.pi/agent/agents/`）不进面板——pi 对它没有加载器，列出来等于把某个扩展的私有布局呈现成这个宿主的一等概念。
- protocol.ts 修改后需同步 host 端（bridge）与 webview 端（main.ts）两侧。
- UI 文案改动要同时更新 `i18n.ts` 的 en 与 zh 两套字典；宿主侧面向用户的文案（原生对话框、QuickPick、transcript 状态提示）全部放在 `src/shared/messages.ts`，通过 `agent/i18n.ts` 的 `t()` / `tf()` 取用，不要在代码里内联英文字面量或 `startsWith("zh")` 判断。有意不本地化的三类：`/` 命令目录（对齐 CLI）、spike 诊断命令（开发者工具）、SDK/模型产生的文本。**渲染方不同则字典不同**：上面两处都是插件自己画的 UI；而由 **VS Code 自己**渲染的清单文案（设置界面、命令面板、应用商店介绍）拿不到 `t()`，只能走清单本地化：`package.json` 里写 `%key%` 占位符，值放在根目录的 `package.nls.json`（英文兼兵底）与 `package.nls.zh-cn.json`（简体中文）；新增清单文案时两个文件都要补，不要在 `package.json` 里内联字面量。
- webview 端改动后 `pnpm verify` 会比对 `scripts/webview-snapshot.txt` 的 DOM 快照；差异即回归信号，确认无误后用 `node scripts/smoke_webview.mjs --update` 更新基线并在 commit 中说明。**单跑 smoke 前必须先 `pnpm build`**：它加载的是 `dist/webview.js`，不重建就是在测旧产物，会给出假的通过。

## 构建与验证

```powershell
pnpm build          # esbuild -> dist/extension.js + dist/webview.js
pnpm typecheck      # tsc --noEmit
pnpm verify         # 构建 + bundle 校验 + 无头冒烟测试
pnpm package:vsix   # 打包 vsix
```

改完代码至少跑 `pnpm build` 和 `pnpm typecheck`。涉及 SDK 加载/打包的改动跑 `pnpm verify`。

## 已知陷阱

- **子会话必须有自己的 `services`（否则毒死父会话的全部扩展）**：扩展是 per-`ResourceLoader` 加载一次的，`getExtensions()` 返回缓存结果，而 `core/agent-session.ts` 用它构造 `ExtensionRunner`、`core/extensions/runner.ts` 的 `bindCore()` 把**当前会话**的动作抄进那个共享 runtime（源码注释："all extension APIs reference this"）。于是共用一份 services 时：子会话一构造就把所有扩展的 `pi.*` 劫持到自己身上，`child.dispose()` 又经 `runner.invalidate()` 给共享 runtime 永久打上 stale 标记（并取消所有 `pi.events` 订阅），此后父会话里任何扩展调 `pi.*` 都抛 "This extension ctx is stale after session replacement or reload"，且**没有复位路径**（只有重建 services 才会重新加载扩展）。CLI 撞不到：`switchSession`/`newSession` 在 dispose 后立刻用工厂造全新 services。因此 `agent/subagent.ts` 的子会话走 `runtime.ts` 的 `createSubagentServices()`——只共享 `modelRuntime` / `settingsManager`（非会话绑定，且带着 auth 与 project-trust 决定），`resourceLoader` 一律新建。**并行时每一路都要各自一份**，不能 N 路共用。扩展 factory 会随之重跑一遍（provider 重复注册是 SDK 定义好的 merge 语义）；模块 factory 有进程级缓存，新 loader 首次 `reload()` 不清缓存，所以开销主要是重扫 skills/prompts/context（并行度上限的一部分理由）。`diagnostics.ts` 的 `subagent isolation` 自检项钉住它：跑完 `createSubagentServices()` + `child.dispose()` 后，父会话的探针扩展仍要能答出**父**会话的名字。

- **重载资源只能走 `AgentSession.reload()`**（同根的另一面）：会话的 `ExtensionRunner` 是构造时从 `resourceLoader.getExtensions()` 建一次的，而 `resourceLoader.reload()` 会产出**全新的** `extensionsResult`（新 runtime + 新实例）。所以“`loader.reload()` + `session.bindExtensions()`”比什么都不做还糟：会话继续用旧实例（新增/修改的扩展永不生效），`bindExtensions()` 还会向旧实例重发一次 `session_start`。`session.reload()` 才会拆旧 runner（shutdown + invalidate）、重载 settings/资源、重建 runner 与工具注册表（宿主 `customTools` 与激活集保留）并重发 `session_start`（reason `reload`）；SDK 三个 mode 的 `/reload` 全是它。它不换 session 对象，所以订阅与 histories 不受影响；但扩展实例换了，因此 `ChatBridge` 经 `beforeSessionStart` 钩子在“新 runner 已建、`session_start` 未发”的空档里清掉 setStatus/setWidget（新实例重新发布，同 `attach()` 的契约），并重建 skill/prompt 索引。`diagnostics.ts` 的 `extension reload` 自检项在两次读取之间改写探针扩展：新工具要出现、旧工具要消失，四个核心工具不能掉（`subagent` 开启时同理）。
- OAuth flow 需在入口静态注册（`registerBunOAuthFlows()`），bundler 无法跟踪 SDK 的动态 import。
- **`import.meta.url` 必须逐模块重建，不能全局指向入口**：打包成 CJS 后 `define` 把它改写成 `__piSdkEntryUrl`，对「定位包根」的用法（docs/examples/themes）没问题；但 `core/extensions/loader.js` 是拿它推**自己所在目录**来拼 jiti alias（`resolve(__dirname, "../..", "index.js")`），统一指向入口会高两层变成 `@earendil-works/index.js`，**所有 import 了 `@earendil-works/pi-coding-agent` 的扩展都会加载失败**（其余功能完全正常，所以很隐蔽）。`esbuild.mjs` 的 `sdkModuleUrlPlugin` 给每个 SDK 模块注入模块作用域的 `__piSdkEntryUrl`（遮蔽 banner 那个全局常量），`diagnostics.ts` 的 `extension sdk import` 自检项在 **bundle 内**加载一个 probe 扩展来钉住它。
- **`dist/node_modules/` 必须自给自足**：pi 扩展经 jiti 加载的是**磁盘上**那份 SDK（alias 锚在 SDK 入口的 `import.meta.url`），而不是 bundle 里那份，所以磁盘副本自己的裸 import（`partial-json`/`yaml`/`chalk`…）也必须随 VSIX 发出去。清单在 `scripts/runtime-packages.mjs`（**单一事实源**，esbuild 与校验共用），只收**静态加载路径的闭包**——provider SDK（anthropic/openai/genai/mistral/bedrock，约 35 MB）由 pi-ai 惰性 import，不入列。这个坑开发时看不见：从仓库跑时 Node 会向上找到项目自己的 `node_modules` 兜底，只有打包成 VSIX（vsce 剥掉根 `node_modules`）后才炸成 `Cannot find module`。`pnpm verify` 的 `scripts/check_extension_runtime.mjs` 在仓库外的沙箱里重建发布布局来钉住它。
- **清单包的版本按 SDK 的视角取，不是根 `node_modules` 的那份**（`runtimePackageSource()`）：hoisted 布局下，插件自己的直接依赖会拿下根位置，把 SDK 需要的版本挤到 `node_modules/@earendil-works/pi-coding-agent/node_modules/` 里。清单服务的是**磁盘上那份 SDK**，所以必须优先取它的嵌套副本；插件自己那份已经进了 bundle，永远不从磁盘读。真实例子：webview 用 highlight.js 11.x 做语法高亮，SDK 需要 10.x，而 11.x 的 exports 已不再开放 SDK 用的 `./lib/index.js`——复制根那份会让磁盘 SDK 直接加载失败（`ERR_PACKAGE_PATH_NOT_EXPORTED`）。**新增与 SDK 同名的直接依赖时先想这一层。**
- undici 是本仓库的**显式直接依赖**（`src/agent/http.ts` 装全局代理 dispatcher）；esbuild `alias` 把所有 `import "undici"`（含 SDK 嵌套副本）收敛到这一份，单副本 + >= 8.7.0（代理转发修复）由 `scripts/check_bundle.py` 断言；升级时保持 >= SDK 自带版本。
- webview CSP 严格：脚本必须带 nonce，样式/图片只允许 `webview.cspSource`。
