# Pi Agent Chat 0.0.19-20260922

## 变更

- 图片附件按当前模型的逐模型缩放配置（models.json 的 `inputLimits.images.resize`）处理，坐标换算说明与模型实际收到的一致。
- 手动重试与继续改为持久化地把被放弃的响应从模型上下文忽略（SDK 0.87.0 机制），transcript 仍然显示它。

## 升级

- Pi SDK（`@earendil-works/pi-ai` / `@earendil-works/pi-coding-agent`）：0.85.1 → 0.87.0。
- `marked`：18.0.11 → 18.0.13。

## 内部

- jiti 不再打进 `dist/extension.js`，改为运行时从随包发行的 `dist/node_modules/` 副本加载（SDK 0.86 扩展加载器的要求）。
- 运行时集合移除 `@mariozechner/clipboard`；SDK 的原生剪贴板改由 `@earendil-works/pi-tui` 自带，原生剪贴板 spike 探针随之删除。
- 非打包运行时集合随 SDK 0.86 依赖树同步：新增 `proxy-agent-negotiate`，移除九个 AWS/Smithy 传递依赖与 `glob`。
- 开发依赖：`@vscode/vsce` 3.9.2 → 4.0.0、`jsdom` 30.0.1 → 30.1.0、`sass` 1.104.0 → 1.104.1、`@types/node` 24.13.3 → 24.13.6。
- 新增 `image resize profile` 自检项，钉住逐模型缩放配置穿透到缩放器。
