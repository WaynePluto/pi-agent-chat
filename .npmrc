# VSIX 打包与 VS Code Extension Host 要求扁平可解析的 node_modules 布局
node-linker=hoisted
shamefully-hoist=true
# lockfile 不写入 tarball 镜像地址，安装时由各环境 registry 解析（GitHub Actions 走默认源）
lockfile-include-tarball-url=false
